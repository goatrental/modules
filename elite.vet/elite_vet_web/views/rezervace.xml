<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Stranka rezervace. Zdrojovy jazyk sablony je ANGLICTINA, ostatni jazyky
         jsou preklady v i18n/*.po — uprava textu tady tedy nerozbije preklady. -->
    <template id="rezervace" name="Elite Vet - Rezervace">
        <t t-call="website.layout">
            <!-- Adresa teze stranky v kazdem jazyce. Prepinac na ne vede primo,
                 protoze pres /website/lang/ to stoji na cookie frontend_lang a ten
                 in-app prohlizec Instagramu a Facebooku neposila tak, jak Odoo ceka.
                 Cestina je vychozi jazyk a na "/" vraci url_for prazdno, proto "/". -->
            <t t-set="ev_jazyk_cs" t-value="('%s' % url_for(request.httprequest.path, lang_code='cs_CZ')) or '/'"/>
            <t t-set="ev_jazyk_de" t-value="('%s' % url_for(request.httprequest.path, lang_code='de_DE')) or '/'"/>
            <t t-set="ev_jazyk_en" t-value="('%s' % url_for(request.httprequest.path, lang_code='en_US')) or '/'"/>
            <t t-set="ev_jazyk_ru" t-value="('%s' % url_for(request.httprequest.path, lang_code='ru_RU')) or '/'"/>
            <t t-set="ev_web" t-value="request.env['elite.vet.setting'].sudo().nastaveni() if 'elite.vet.setting' in request.env else None"/>
            <t t-set="ev_hodiny" t-value="request.env['elite.vet.opening.hours'].sudo().search([]) if 'elite.vet.opening.hours' in request.env else []"/>
        <t t-set="ev_dotazy" t-value="request.env['elite.vet.faq'].sudo().search([]) if 'elite.vet.faq' in request.env else []"/>
            <t t-set="no_header" t-value="true"/>
            <t t-set="no_footer" t-value="true"/>
            <t t-set="additional_title">Book an appointment | Elite Vet Karlovy Vary</t>
            <t t-set="meta_description">Book online at the Elite Vet veterinary clinic in Karlovy Vary. Choose a free appointment with our doctors, or call the reception. Answers to the most common questions before your visit.</t>
            <meta name="theme-color" content="#1a2744"/>

            <style>
                :root {
                    --ev-bg: #ffffff;
                    --ev-fg: #1a2744;
                    --ev-primary: #1a2744;
                    --ev-primary-dark: #111b30;
                    --ev-primary-light: #e8edf4;
                    --ev-accent: #2b7870;
                    --ev-accent-dark: #1f5a54;
                    --ev-accent-light: #e0f2f0;
                    --ev-muted: #f7f9fb;
                    --ev-border: #e2e8f0;
                    --ev-text-secondary: #64748b;
                }

                /* ============ JAZYKOVÁ BRÁNA (intro při načtení — výběr jazyka) ============ */
                /* zobrazí se jen jednou za návštěvu; guard níže nastaví .ev-noSplash */
                html.ev-noSplash .ev-langgate { display: none !important; }
                .ev-langgate { position: fixed; inset: 0; z-index: 9999; display: flex; align-items: center; justify-content: center; padding: 24px; background: rgba(17,27,48,.55); -webkit-backdrop-filter: blur(6px); backdrop-filter: blur(6px); animation: evGateIn .35s ease both; }
                @keyframes evGateIn { from { opacity: 0; } to { opacity: 1; } }
                .ev-langgate-card { width: 100%; max-width: 460px; background: #fff; border-radius: 26px; padding: 38px 32px 32px; text-align: center; box-shadow: 0 30px 80px -20px rgba(9,15,28,.5); animation: evGateCard .5s cubic-bezier(.2,.8,.25,1) both; }
                @keyframes evGateCard { from { opacity: 0; transform: translateY(16px) scale(.96); } to { opacity: 1; transform: none; } }
                .ev-langgate-logo { width: auto; height: auto; max-width: 190px; max-height: 96px; object-fit: contain; margin: 0 auto 18px; display: block; }
                .vet-page .ev-langgate-title { font-size: 23px; line-height: 1.22; font-weight: 800; color: var(--ev-primary); }
                .ev-langgate-sub { margin-top: 10px; color: var(--ev-text-secondary); font-size: 14.5px; line-height: 1.5; }
                .ev-langgate-grid { margin-top: 24px; display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
                .ev-langgate-opt { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 4px; padding: 18px 10px; border-radius: 16px; border: 1.5px solid var(--ev-border); background: #fff; color: var(--ev-primary) !important; transition: border-color .2s, background-color .2s, box-shadow .2s, transform .1s; }
                .ev-langgate-opt:hover { border-color: var(--ev-accent); background: var(--ev-accent-light); box-shadow: 0 10px 24px -14px rgba(91,168,160,.7); }
                .ev-langgate-opt:active { transform: scale(.98); }
                .ev-langgate-code { font-size: 20px; font-weight: 800; letter-spacing: .04em; color: var(--ev-primary); }
                .ev-langgate-name { font-size: 13px; font-weight: 600; color: var(--ev-text-secondary); }
                @media (max-width: 480px) {
                    .ev-langgate-card { padding: 30px 20px 24px; border-radius: 22px; }
                    .ev-langgate-logo { max-width: 150px; max-height: 78px; }
                    .vet-page .ev-langgate-title { font-size: 20px; }
                    .ev-langgate-opt { padding: 15px 8px; }
                }

                /* ============ HIDE ODOO DEFAULT HEADER (aggressive) ============ */
                body > header,
                #wrapwrap > header,
                #wrapwrap > div > header,
                header#top,
                header.o_header_standard,
                header.o_header_affix,
                header.o_main_header,
                .o_header_standard,
                .o_header_affix,
                .o_main_navbar,
                .o_top_fixed_element,
                body > footer,
                #wrapwrap > footer,
                footer#footer,
                footer.o_footer,
                .o_footer,
                .o_footer_copyright { display: none !important; visibility: hidden !important; height: 0 !important; max-height: 0 !important; overflow: hidden !important; opacity: 0 !important; pointer-events: none !important; }
                body, html { padding-top: 0 !important; margin-top: 0 !important; }

                /* ============ TOPBAR (vlastní sticky nav) ============ */
                /* plná bílá (ne průhledná) — logo je JPEG s bílým pozadím, na poloprůhledné liště by svítilo jako čtverec */
                .ev-topbar { position: fixed; top: 0; left: 0; right: 0; z-index: 100; background: #ffffff; border-bottom: 1px solid var(--ev-border); transition: transform .3s ease; -webkit-backface-visibility: hidden; backface-visibility: hidden; will-change: transform; }
                .ev-topbar.ev-nav-hidden { transform: translateY(-100%); }
                .ev-topbar-inner { display: flex; align-items: center; justify-content: space-between; height: 68px; }
                .ev-topbar-logo { display: inline-flex; align-items: center; gap: 0; font-size: 20px; font-weight: 800; color: var(--ev-primary); letter-spacing: -0.01em; }
                .ev-topbar-logo:hover { color: var(--ev-primary); }
                .ev-topbar-img { height: 44px; width: auto; max-width: 80px; object-fit: contain; display: block; border-radius: 8px; margin-right: 10px; }
                .ev-topbar-nav { display: flex; align-items: center; gap: 28px; }
                .ev-topbar-nav a { font-size: 14px; font-weight: 500; color: var(--ev-text-secondary); transition: color .2s; }
                .ev-topbar-nav a:hover { color: var(--ev-accent); }
                .ev-topbar-cta { display: inline-flex; align-items: center; gap: 8px; background: var(--ev-accent); color: #fff; padding: 9px 18px; border-radius: 9999px; font-size: 14px; font-weight: 600; transition: background-color .2s; }
                .ev-topbar-cta:hover { background: var(--ev-accent-dark); color: #fff; }
                .vet-page .ev-topbar-nav a.ev-nav-rez { color: var(--ev-accent-dark); font-weight: 600; border-bottom: 2px solid var(--ev-accent); padding-bottom: 3px; }
                .vet-page .ev-topbar-nav a.ev-nav-rez:hover { color: var(--ev-accent-dark); border-bottom-color: var(--ev-accent-dark); }

                /* ============ BURGER MENU (mobile, pure CSS) ============ */
                .ev-burger { position: relative; display: none; }
                .ev-burger > summary { list-style: none; cursor: pointer; display: inline-flex; align-items: center; justify-content: center; width: 44px; height: 44px; border-radius: 12px; background: var(--ev-accent); color: #fff; transition: background-color .2s; }
                .ev-burger > summary::-webkit-details-marker { display: none; }
                .ev-burger > summary:hover { background: var(--ev-accent-dark); }
                .ev-burger-panel { position: absolute; top: calc(100% + 10px); right: 0; min-width: 280px; max-width: calc(100vw - 24px); background: #fff; border: 1px solid var(--ev-border); border-radius: 18px; padding: 14px; box-shadow: 0 18px 40px -12px rgba(17,27,48,.28); z-index: 999; animation: evCallInTop .18s ease-out; display: flex; flex-direction: column; gap: 4px; }
                .ev-burger-link { display: flex; align-items: center; gap: 14px; padding: 16px 18px; border-radius: 14px; color: var(--ev-primary); font-size: 16px; font-weight: 600; transition: background-color .15s; }
                .ev-burger-link:hover { background: var(--ev-muted); color: var(--ev-primary); }
                .ev-burger-link svg { color: var(--ev-accent); flex-shrink: 0; }
                .ev-burger-divider { height: 1px; background: var(--ev-border); margin: 6px 4px; }
                .ev-burger-tel { display: flex; align-items: center; gap: 12px; padding: 11px 14px; border-radius: 12px; transition: background-color .15s; color: var(--ev-primary); }
                .ev-burger-tel:hover { background: var(--ev-accent-light); color: var(--ev-primary); }
                .ev-burger-tel .ev-call-icon { width: 36px; height: 36px; }
                .ev-burger-tel .ev-call-num { font-size: 14px; font-weight: 700; }
                .ev-burger-tel .ev-call-lbl { font-size: 11px; }

                html { scroll-behavior: smooth; }
                .vet-page { font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif; color: var(--ev-fg); background: var(--ev-bg); line-height: 1.5; }
                .vet-page * { box-sizing: border-box; }
                .vet-page a { color: inherit; text-decoration: none; }
                .vet-page p { margin: 0; }
                .vet-page h1, .vet-page h2, .vet-page h3 { margin: 0; letter-spacing: -0.02em; color: var(--ev-primary); font-weight: 700; }
                .vet-page section.o_colored_level { padding-left: 0; padding-right: 0; }

                .ev-container { width: 100%; max-width: 1200px; margin: 0 auto; padding: 0 24px; }

                .ev-badge { display: inline-flex; align-items: center; gap: 8px; background: var(--ev-accent-light); color: var(--ev-accent); padding: 6px 16px; border-radius: 9999px; font-size: 14px; font-weight: 500; margin-bottom: 16px; }
                .ev-section-head { text-align: center; max-width: 640px; margin: 0 auto 64px; }
                .ev-section-head h2 { font-size: clamp(28px, 4vw, 38px); line-height: 1.15; }
                .ev-section-head p { margin-top: 16px; color: var(--ev-text-secondary); font-size: 18px; }
                .ev-accent-text { color: var(--ev-accent); }

                .ev-btn { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 14px 28px; border-radius: 9999px; font-size: 15px; font-weight: 600; transition: background-color .2s, color .2s, border-color .2s, box-shadow .2s; cursor: pointer; border: 0; }
                .ev-btn-primary { background: #fff; color: var(--ev-accent); border: 1.5px solid var(--ev-accent); font-weight: 700; box-shadow: none; }
                .ev-btn-primary:hover { background: var(--ev-accent); color: #fff; border-color: var(--ev-accent); }
                .ev-btn-outline { background: transparent; color: var(--ev-primary); border: 2px solid var(--ev-border); }
                .ev-btn-outline:hover { border-color: var(--ev-accent); color: var(--ev-accent); }

                @keyframes evBtnPulse {
                    0% { box-shadow: 0 0 0 0 rgba(91,168,160,.55); }
                    70% { box-shadow: 0 0 0 14px rgba(91,168,160,0); }
                    100% { box-shadow: 0 0 0 0 rgba(91,168,160,0); }
                }
                @keyframes evBtnGlow {
                    0%, 100% { border-color: var(--ev-accent); color: var(--ev-accent); }
                    50% { border-color: var(--ev-accent-dark); color: var(--ev-accent-dark); }
                }
                .ev-btn-pulse {
                    border-color: var(--ev-accent) !important;
                    color: var(--ev-accent) !important;
                    animation: evBtnPulse 1.8s ease-out infinite, evBtnGlow 1.8s ease-in-out infinite;
                }
                .ev-btn-pulse:hover { animation: none; background: var(--ev-accent); color: #fff !important; border-color: var(--ev-accent) !important; box-shadow: 0 6px 16px -4px rgba(91,168,160,.5); }

                /* ============ CALL POPUP (pure CSS via &lt;details&gt;) ============ */
                /* ============ PŘEPÍNAČ JAZYKŮ (odkazy rovnou na /cs, /de, /en, /ru) ============ */
                .ev-lang { position: relative; display: inline-flex; }
                .ev-lang > summary { list-style: none; cursor: pointer; display: inline-flex; align-items: center; gap: 7px; padding: 8px 13px; border-radius: 9999px; border: 1px solid var(--ev-border); background: #fff; color: var(--ev-primary); font-size: 13px; font-weight: 700; letter-spacing: .02em; transition: border-color .2s, background-color .2s, color .2s; }
                .ev-lang > summary::-webkit-details-marker { display: none; }
                .ev-lang > summary::marker { content: ""; }
                .ev-lang > summary:hover { border-color: var(--ev-accent); background: var(--ev-accent-light); color: var(--ev-accent-dark); }
                .ev-lang > summary svg { flex-shrink: 0; }
                .ev-lang-caret { opacity: .6; transition: transform .25s ease; }
                .ev-lang[open] > summary .ev-lang-caret { transform: rotate(180deg); }
                details.ev-lang[open] { z-index: 9999; }
                .ev-lang-panel { position: absolute; top: calc(100% + 10px); right: 0; min-width: 170px; display: flex; flex-direction: column; gap: 2px; background: #fff; border: 1px solid var(--ev-border); border-radius: 14px; padding: 6px; box-shadow: 0 18px 40px -12px rgba(17,27,48,.28); z-index: 9999; animation: evCallInTop .18s ease-out; }
                .ev-lang-opt { display: block; padding: 10px 14px; border-radius: 10px; font-size: 14px; font-weight: 600; color: var(--ev-text-secondary); white-space: nowrap; transition: background-color .15s, color .15s; }
                .ev-lang-opt:hover { background: var(--ev-muted); color: var(--ev-primary); }
                .ev-lang-opt.is-active { background: var(--ev-accent-light); color: var(--ev-accent-dark); }
                /* odznak aktuálního jazyka — pure CSS podle html[lang], bez QWeb (aby šel text v editoru upravovat) */
                .ev-lang-code { display: none; }
                .ev-lang-code-cs { display: inline; }
                html[lang^="de"] .ev-lang-code-cs,
                html[lang^="en"] .ev-lang-code-cs,
                html[lang^="ru"] .ev-lang-code-cs { display: none; }
                html[lang^="de"] .ev-lang-code-de,
                html[lang^="en"] .ev-lang-code-en,
                html[lang^="ru"] .ev-lang-code-ru { display: inline; }
                .ev-call { position: relative; display: inline-flex; }
                .ev-call.ev-call--full { display: flex; width: 100%; }
                .ev-call > summary { list-style: none; cursor: pointer; display: inline-flex; align-items: center; justify-content: center; gap: 8px; font-weight: 600; transition: background-color .2s, color .2s, border-color .2s, box-shadow .2s; }
                .ev-call > summary::-webkit-details-marker { display: none; }
                .ev-call-panel { position: absolute; top: calc(100% + 10px); left: 50%; transform: translateX(-50%); min-width: 260px; background: #fff; border: 1px solid var(--ev-border); border-radius: 16px; padding: 8px; box-shadow: 0 18px 40px -12px rgba(17,27,48,.28); z-index: 9999; animation: evCallIn .18s ease-out; }
                details.ev-call[open] { position: relative; z-index: 9999; }
                @keyframes evCallIn { from { opacity: 0; transform: translate(-50%, -6px); } to { opacity: 1; transform: translate(-50%, 0); } }
                .ev-call-item { display: flex; align-items: center; gap: 12px; padding: 11px 14px; border-radius: 12px; transition: background-color .15s; color: var(--ev-primary); }
                .ev-call-item:hover { background: var(--ev-accent-light); color: var(--ev-primary); }
                .ev-call-item + .ev-call-item { margin-top: 4px; }
                .ev-call-icon { width: 38px; height: 38px; border-radius: 10px; background: var(--ev-accent-light); color: var(--ev-accent); display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
                .ev-call-num { font-size: 15px; font-weight: 700; color: var(--ev-primary); letter-spacing: 0.01em; }
                .ev-call-lbl { font-size: 12px; color: var(--ev-text-secondary); margin-top: 1px; }

                .ev-call--primary > summary { background: var(--ev-accent); color: #fff; padding: 14px 26px; border-radius: 9999px; font-size: 15px; box-shadow: 0 10px 24px -8px rgba(91,168,160,.45); }
                .ev-call--primary > summary:hover { background: var(--ev-accent-dark); }
                .ev-call--outline > summary { background: transparent; color: var(--ev-primary); border: 2px solid var(--ev-border); padding: 12px 24px; border-radius: 9999px; font-size: 15px; }
                .ev-call--outline > summary:hover { border-color: var(--ev-accent); color: var(--ev-accent); }
                .ev-call--topbar > summary { background: var(--ev-accent); color: #fff; padding: 9px 18px; border-radius: 9999px; font-size: 14px; }
                .ev-call--topbar > summary:hover { background: var(--ev-accent-dark); }
                .ev-call--topbar > .ev-call-panel { left: auto !important; right: 0 !important; transform: none !important; animation: evCallInTop .18s ease-out; }
                @keyframes evCallInTop { from { opacity: 0; transform: translateY(-6px); } to { opacity: 1; transform: translateY(0); } }
                .ev-mission-card > details.ev-call--mission,
                .ev-call.ev-call--mission { display: flex !important; justify-content: center !important; align-items: center !important; width: 100% !important; margin: 20px auto 0 auto !important; text-align: center !important; }
                .ev-call--mission > summary { background: var(--ev-accent) !important; color: #fff !important; padding: 14px 24px !important; border-radius: 9999px !important; font-weight: 600 !important; font-size: 15px !important; }
                .ev-call--mission > summary:hover { background: var(--ev-accent-dark) !important; }
                .ev-call--full > summary { width: 100%; padding: 14px 24px; border-radius: 12px; background: var(--ev-accent); color: #fff; font-size: 14px; }
                .ev-call--full > summary:hover { background: var(--ev-accent-dark); }

                /* přímé volání — telefonní tlačítka jsou odkazy tel: místo rozbalovacího details */
                a.ev-call { display: inline-flex; align-items: center; justify-content: center; gap: 8px; font-weight: 600; text-decoration: none; transition: background-color .2s, color .2s, border-color .2s, box-shadow .2s; }
                a.ev-call--topbar { background: var(--ev-accent); color: #fff !important; padding: 9px 18px; border-radius: 9999px; font-size: 14px; }
                a.ev-call--topbar:hover { background: var(--ev-accent-dark); color: #fff !important; }
                a.ev-call--outline { background: transparent; color: var(--ev-primary) !important; border: 2px solid var(--ev-border); padding: 12px 24px; border-radius: 9999px; font-size: 15px; }
                a.ev-call--outline:hover { border-color: var(--ev-accent); color: var(--ev-accent) !important; }
                a.ev-call--mission { background: var(--ev-accent) !important; color: #fff !important; padding: 14px 24px !important; border-radius: 9999px !important; font-weight: 600 !important; font-size: 15px !important; }
                a.ev-call--mission:hover { background: var(--ev-accent-dark) !important; }
                a.ev-call--full { width: 100%; padding: 14px 24px; border-radius: 12px; background: var(--ev-accent); color: #fff !important; font-size: 14px; }
                a.ev-call--full:hover { background: var(--ev-accent-dark); color: #fff !important; }

                /* ============ COUNTDOWN HERO — DEN OTEVŘENÝCH DVEŘÍ ============ */
                /* fotka kliniky jako pozadí + tmavý scrim, aby texty zůstaly čitelné */
                /* lvh = velký viewport (jako by lišty prohlížeče byly schované) — na iOS jinak dole vykoukne bílý pruh */
                .ev-cd { --cd-pt: 68px; --cd-pb: 34px; position: relative; min-height: 100vh; min-height: 100svh; display: flex; align-items: center; justify-content: center; padding: var(--cd-pt) 0 var(--cd-pb); overflow: hidden;
                    background:
                        radial-gradient(circle at 12% 10%, rgba(91,168,160,.10) 0%, transparent 42%),
                        radial-gradient(circle at 88% 90%, rgba(91,168,160,.08) 0%, transparent 44%),
                        #ffffff;
                }
                .ev-cd-inner { position: relative; z-index: 2; width: 100%; display: flex; flex-direction: column; align-items: center; text-align: center; }

                /* leták vycentrovaný v heru — na PC se stropem výšky, ať se s odpočtem vejde na jednu stránku; mobil = telefonní na plnou šířku */
                .ev-flyer { width: 100%; display: flex; justify-content: center; }
                .ev-flyer img { display: block; width: auto; max-width: 100%; height: auto; max-height: 70vh; }
                .ev-cd-count-label { margin-top: 24px; font-size: clamp(11px, 1.1vw, 13px); font-weight: 800; letter-spacing: .18em; text-transform: uppercase; color: var(--ev-text-secondary); }
                @media (max-width: 820px) { .ev-flyer { width: 100vw; } .ev-flyer img { width: 100%; max-width: none; max-height: 82vh; } .ev-cd-count-label { margin-top: 16px; } }

                /* tlačítko „Kde?" pod odpočtem — sroluje na mapu */
                .ev-cd-where { margin-top: 22px; display: inline-flex; align-items: center; gap: 8px; background: var(--ev-accent); color: #fff !important; padding: 12px 28px; border-radius: 9999px; font-size: 15px; font-weight: 700; box-shadow: 0 12px 28px -12px rgba(91,168,160,.6); transition: background-color .2s, transform .1s; }
                .ev-cd-where:hover { background: var(--ev-accent-dark); color: #fff !important; }
                .ev-cd-where:active { transform: scale(.98); }
                .ev-cd-where svg { flex-shrink: 0; }
                /* samostatná malá sekce jen s tlačítkem Karlovy Vary */
                .ev-where-sec { background: #fff; padding: 30px 0 46px; text-align: center; }
                .ev-where-sec .ev-cd-where { margin-top: 0; }
                @media (max-width: 820px) { .ev-cd-where { padding: 11px 24px; font-size: 14px; } .ev-where-sec { padding: 22px 0 34px; } }

                .ev-cd-badge { display: inline-flex; align-items: center; gap: 12px; margin-top: 26px; background: rgba(91,168,160,.22); border: 1.5px solid rgba(91,168,160,.6); color: #cfeeea; padding: 13px 32px; border-radius: 9999px; font-size: clamp(18px, 2.3vw, 26px); font-weight: 800; letter-spacing: .06em; text-transform: uppercase; }
                .ev-cd-badge svg { flex-shrink: 0; width: 24px; height: 24px; }
                /* Červený upoutávkový pruh — pohotovost 24/7 už brzy (v poznámce pod odpočtem) */
                .ev-cd-emergency { display: inline-flex; align-items: center; gap: 9px; background: rgba(220,38,38,.16); border: 1px solid rgba(248,113,113,.55); color: #fecaca; padding: 8px 20px; border-radius: 9999px; font-size: 13.5px; font-weight: 800; letter-spacing: .04em; text-transform: uppercase; box-shadow: 0 0 0 0 rgba(220,38,38,.5); animation: evEmergencyPulse 2s ease-out infinite; }
                .ev-cd-emergency { cursor: pointer; }
                .ev-cd-emergency svg { flex-shrink: 0; color: #f87171; }
                .ev-cd-emergency-dot { width: 9px; height: 9px; border-radius: 9999px; background: #ef4444; flex-shrink: 0; box-shadow: 0 0 0 0 rgba(239,68,68,.7); animation: evEmergencyDot 1.4s ease-out infinite; }
                @keyframes evEmergencyPulse { 0% { box-shadow: 0 0 0 0 rgba(220,38,38,.45); } 70% { box-shadow: 0 0 0 12px rgba(220,38,38,0); } 100% { box-shadow: 0 0 0 0 rgba(220,38,38,0); } }
                @keyframes evEmergencyDot { 0% { box-shadow: 0 0 0 0 rgba(239,68,68,.75); } 70% { box-shadow: 0 0 0 8px rgba(239,68,68,0); } 100% { box-shadow: 0 0 0 0 rgba(239,68,68,0); } }
                /* ============ COACH-MARK (po otevření: ztmavit stránku, ukázat bublinu u 24/7) ============ */
                .ev-coach { position: fixed; inset: 0; z-index: 101; background: rgba(9,15,28,.74); -webkit-backdrop-filter: blur(2px); backdrop-filter: blur(2px); opacity: 0; visibility: hidden; transition: opacity .35s ease, visibility .35s ease; cursor: pointer; }
                .ev-coach.ev-coach-on { opacity: 1; visibility: visible; }
                /* bublina s vysvětlením — sedí nad kolečkem 24/7 vpravo dole */
                .ev-coach-card { position: fixed; right: 22px; bottom: 104px; z-index: 103; width: min(310px, calc(100vw - 44px)); background: #fff; border-radius: 18px; padding: 18px; text-align: left; box-shadow: 0 26px 64px -16px rgba(9,15,28,.6); opacity: 0; transform: translateY(12px) scale(.96); transition: opacity .3s ease .05s, transform .3s ease .05s; pointer-events: none; }
                .ev-coach.ev-coach-on .ev-coach-card { opacity: 1; transform: none; pointer-events: auto; }
                .ev-coach-card::after { content: ""; position: absolute; right: 30px; bottom: -8px; width: 16px; height: 16px; background: #fff; transform: rotate(45deg); border-radius: 3px; box-shadow: 4px 4px 10px -4px rgba(9,15,28,.25); }
                .vet-page .ev-coach-title { display: flex; align-items: center; gap: 9px; font-size: 16px; font-weight: 800; color: var(--ev-primary); line-height: 1.2; }
                .ev-coach-title svg { flex-shrink: 0; color: #dc2626; }
                .ev-coach-text { margin-top: 9px; font-size: 13.5px; line-height: 1.55; color: var(--ev-text-secondary); }
                .ev-coach-btn { margin-top: 15px; display: inline-flex; align-items: center; justify-content: center; gap: 8px; width: 100%; background: var(--ev-accent); color: #fff !important; border: 0; border-radius: 9999px; padding: 11px 20px; font-size: 14px; font-weight: 700; cursor: pointer; transition: background-color .2s; }
                .ev-coach-btn:hover { background: var(--ev-accent-dark); }
                /* šedé neaktivní tlačítko Volat — číslo zatím není připravené */
                .ev-coach-call { margin-top: 8px; display: inline-flex; align-items: center; justify-content: center; gap: 8px; width: 100%; background: #eef1f5; color: #94a3b8 !important; border: 1px solid var(--ev-border); border-radius: 9999px; padding: 11px 20px; font-size: 14px; font-weight: 700; cursor: not-allowed; }
                .ev-coach-call svg { flex-shrink: 0; opacity: .8; }
                .ev-coach-call-soon { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: .03em; opacity: .85; }

                /* ============ PLOVOUCÍ 24/7 BUTTON (fixed vpravo dole, blikající, zatím neklikací) ============ */
                .ev-fab247-wrap { position: fixed; right: 22px; bottom: 22px; z-index: 102; display: flex; flex-direction: column; align-items: center; gap: 6px; pointer-events: none; }
                .vet-page .ev-fab247 { position: relative; pointer-events: auto; width: 68px; height: 68px; border-radius: 50%; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 1px; background: linear-gradient(160deg, #ef4444, #dc2626); color: #fff !important; text-decoration: none; border: 2px solid rgba(255,255,255,.9); box-shadow: 0 12px 26px -8px rgba(220,38,38,.7); animation: evFab247 1.5s ease-out infinite; -webkit-user-select: none; user-select: none; transition: transform .15s; }
                .vet-page a.ev-fab247:hover { transform: scale(1.06); color: #fff !important; }
                .ev-fab247-phone { color: #fff; opacity: .95; flex-shrink: 0; margin-bottom: 2px; }
                .ev-fab247-num { font-size: 18px; font-weight: 800; line-height: 1; letter-spacing: .01em; }
                @keyframes evFab247 { 0% { box-shadow: 0 12px 26px -8px rgba(220,38,38,.7), 0 0 0 0 rgba(220,38,38,.55); } 70% { box-shadow: 0 12px 26px -8px rgba(220,38,38,.7), 0 0 0 18px rgba(220,38,38,0); } 100% { box-shadow: 0 12px 26px -8px rgba(220,38,38,.7), 0 0 0 0 rgba(220,38,38,0); } }
                @media (max-width: 768px) {
                    .ev-fab247-wrap { right: 14px; bottom: 14px; }
                    .vet-page .ev-fab247 { width: 60px; height: 60px; }
                    .ev-fab247-phone { width: 16px; height: 16px; }
                    .ev-fab247-num { font-size: 15px; }
                }
                .vet-page .ev-cd-title { margin-top: 18px; font-size: clamp(28px, 4.6vw, 54px); line-height: 1.08; font-weight: 800; color: #fff; letter-spacing: -0.02em; }
                .vet-page .ev-cd-title span { color: var(--ev-accent); }
                .ev-cd-date { margin-top: 12px; display: inline-flex; align-items: center; justify-content: center; flex-wrap: wrap; text-align: center; line-height: 1.3; gap: 6px 11px; color: #fff; font-size: clamp(18px, 2.5vw, 28px); font-weight: 800; background: rgba(255,255,255,.10); border: 1px solid rgba(255,255,255,.2); padding: 11px 26px; border-radius: 9999px; }
                .ev-cd-date svg { color: var(--ev-accent); flex-shrink: 0; }

                .ev-cd-clock { margin-top: 12px; display: flex; align-items: stretch; justify-content: center; gap: 12px; width: 100%; max-width: 680px; }
                .ev-cd-unit { flex: 1 1 0; min-width: 0; background: var(--ev-muted); border: 1px solid var(--ev-border); border-radius: 18px; padding: 14px 8px 12px; box-shadow: 0 18px 40px -22px rgba(17,27,48,.35); }
                .ev-cd-num { font-size: clamp(30px, 4.8vw, 58px); line-height: 1; font-weight: 800; color: var(--ev-primary); letter-spacing: -0.045em; font-variant-numeric: tabular-nums; }
                .ev-cd-lbl { margin-top: 8px; font-size: clamp(10px, 1vw, 12.5px); font-weight: 700; letter-spacing: .14em; text-transform: uppercase; color: var(--ev-text-secondary); }
                .ev-cd-unit--sec .ev-cd-num { color: var(--ev-accent); }

                .ev-cd-note { margin-top: 18px; display: flex; flex-direction: column; align-items: center; gap: 10px; }
                .ev-cd-note-live { display: none; font-size: 13.5px; }
                .ev-cd-clock-live + .ev-cd-note .ev-cd-note-live { display: block; color: var(--ev-accent); font-weight: 700; }
                .ev-cd-addr { display: inline-flex; align-items: center; gap: 8px; padding: 9px 20px; border-radius: 9999px; background: rgba(255,255,255,.07); border: 1px solid rgba(255,255,255,.22); color: rgba(255,255,255,.88) !important; font-size: 14px; font-weight: 600; transition: background-color .2s, border-color .2s, color .2s; }
                .ev-cd-addr:hover { background: rgba(91,168,160,.22); border-color: var(--ev-accent); color: #fff !important; }
                .ev-cd-addr svg { color: var(--ev-accent); flex-shrink: 0; }

                .ev-cd-text { margin-top: 30px; max-width: 780px; }
                .ev-cd-text p { color: rgba(255,255,255,.78); font-size: clamp(14.5px, 1.35vw, 17px); line-height: 1.65; }
                .ev-cd-text p + p { margin-top: 14px; }
                .ev-cd-text strong { color: #fff; font-weight: 700; }

                .ev-cd-cta { margin-top: 22px; display: flex; flex-wrap: wrap; gap: 12px; justify-content: center; align-items: center; }
                .ev-cd-btn { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 14px 26px; border-radius: 9999px; font-size: 15px; font-weight: 700; transition: background-color .2s, border-color .2s, color .2s; }
                .ev-cd-btn-ghost { background: transparent; color: #fff !important; border: 1.5px solid rgba(255,255,255,.28); }
                .ev-cd-btn-ghost:hover { border-color: var(--ev-accent); color: var(--ev-accent) !important; }
                .ev-cd-cta > .ev-call--primary > summary { box-shadow: none; }
                /* CTA sedí u spodní hrany sekce — panel s telefony otevírej nahoru, ať se nikde neořízne */
                .ev-cd-cta > .ev-call > .ev-call-panel { top: auto; bottom: calc(100% + 10px); animation: evCallInUp .18s ease-out; }
                @keyframes evCallInUp { from { opacity: 0; transform: translate(-50%, 6px); } to { opacity: 1; transform: translate(-50%, 0); } }

                /* Sociální sítě — kulatá tlačítka v brandových barvách */
                .ev-cd-social { margin-top: 38px; display: flex; justify-content: center; gap: 12px; }
                .ev-cd-soc { display: inline-flex; align-items: center; justify-content: center; width: 54px; height: 54px; border-radius: 50%; color: #fff !important; text-shadow: none;
                    transition: filter .2s, box-shadow .2s, transform .2s; }
                .ev-cd-soc:hover { filter: brightness(1.12); transform: translateY(-2px); color: #fff !important; }
                .ev-cd-soc svg { width: 26px; height: 26px; flex-shrink: 0; }
                .ev-cd-ig { background: linear-gradient(45deg, #f09433 0%, #e6683c 22%, #dc2743 48%, #cc2366 72%, #bc1888 100%); box-shadow: 0 12px 28px -12px rgba(220,39,67,.75); }
                .ev-cd-ig:hover { box-shadow: 0 18px 36px -12px rgba(220,39,67,.9); }
                .ev-cd-fb { background: #1877f2; box-shadow: 0 12px 28px -12px rgba(24,119,242,.75); }
                .ev-cd-fb:hover { box-shadow: 0 18px 36px -12px rgba(24,119,242,.9); }

                /* ============ HERO ============ */
                .ev-hero { position: relative; height: 100vh; min-height: 720px; display: flex; align-items: center; padding: 100px 0 70px; overflow: hidden; background: #ffffff; }
                .ev-hero-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 56px; align-items: center; }
                .ev-hero h1 { font-size: clamp(30px, 4.4vw, 52px); line-height: 1.08; font-weight: 700; }
                .ev-hero-logo-mobile { display: none; }
                .ev-hero-location { display: inline-flex; align-items: center; gap: 8px; margin-top: 14px; padding: 7px 16px; background: rgba(91,168,160,.12); border: 1px solid rgba(91,168,160,.3); border-radius: 9999px; color: var(--ev-primary); font-size: 15px; font-weight: 600; }
                .ev-hero-location svg { color: var(--ev-accent); flex-shrink: 0; }
                .ev-hero-lead { margin-top: 18px; font-size: clamp(15px, 1.4vw, 18px); color: var(--ev-text-secondary); line-height: 1.55; max-width: 560px; }
                .ev-hero-cta { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 26px; }
                .ev-hero-stats { display: flex; gap: 28px; margin-top: 32px; align-items: center; }
                .ev-hero-stat-num { font-size: 28px; font-weight: 700; color: var(--ev-primary); }
                .ev-hero-stat-lbl { font-size: 14px; color: var(--ev-text-secondary); margin-top: 2px; }
                .ev-hero-divider { width: 1px; height: 36px; background: var(--ev-border); }

                .ev-hero-card { position: relative; border-radius: 24px; overflow: hidden; background: linear-gradient(135deg, var(--ev-primary), var(--ev-primary-dark)); border: 1px solid var(--ev-border); box-shadow: 0 30px 60px -20px rgba(91,168,160,.18); padding: 26px; display: flex; flex-direction: column; gap: 14px; min-width: 0; }
                .ev-blob-1 { position: absolute; top: -50px; right: -50px; width: 180px; height: 180px; border-radius: 9999px; background: rgba(91,168,160,.15); pointer-events: none; }
                .ev-blob-2 { position: absolute; bottom: -40px; left: -40px; width: 120px; height: 120px; border-radius: 9999px; background: rgba(91,168,160,.1); pointer-events: none; }
                .ev-hire-pill { position: relative; align-self: flex-start; display: inline-flex; align-items: center; gap: 6px; background: rgba(91,168,160,.18); color: var(--ev-accent); padding: 5px 12px; border-radius: 9999px; font-size: 12px; font-weight: 600; letter-spacing: 0.02em; }
                .ev-hire-head { position: relative; }
                .ev-hire-title { font-size: 19px; font-weight: 700; color: #fff; line-height: 1.25; }
                .ev-hire-sub { color: rgba(255,255,255,.6); font-size: 13px; margin-top: 6px; line-height: 1.5; }
                .ev-hero-feat { position: relative; display: flex; flex-direction: column; gap: 8px; }
                .ev-feat-row { display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,.07); -webkit-backdrop-filter: blur(8px); backdrop-filter: blur(8px); border-radius: 12px; padding: 11px 13px; border: 1px solid rgba(255,255,255,.08); }
                .ev-feat-icon { width: 36px; height: 36px; border-radius: 10px; background: rgba(91,168,160,.28); display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
                .ev-feat-title { font-size: 13.5px; font-weight: 600; color: #fff; }
                .ev-feat-desc { font-size: 11.5px; color: rgba(255,255,255,.6); margin-top: 1px; }
                .ev-hire-perk { position: relative; display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,.92); border-radius: 14px; padding: 12px 14px; border: 1px solid var(--ev-border); }
                .ev-hire-perk svg { flex-shrink: 0; }
                .ev-hire-perk-title { font-size: 13.5px; font-weight: 700; color: var(--ev-primary); }
                .ev-hire-perk-sub { font-size: 11.5px; color: var(--ev-text-secondary); margin-top: 1px; }
                .ev-hire-actions { position: relative; display: flex; flex-wrap: wrap; gap: 10px; }
                .ev-hire-cta { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 12px 18px; border-radius: 9999px; font-weight: 600; font-size: 14px; transition: background-color .2s, border-color .2s, color .2s; }
                .ev-hire-cta-primary { background: rgba(91,168,160,.35); color: #fff !important; border: 1.5px solid var(--ev-accent); font-weight: 700; flex: 1; min-width: 0; }
                .ev-hire-cta-primary:hover { background: var(--ev-accent); color: #fff !important; border-color: var(--ev-accent); }
                .ev-hire-cta-ghost { background: rgba(91,168,160,.35); color: #fff !important; border: 1.5px solid var(--ev-accent); font-weight: 700; }
                .ev-hire-cta-ghost:hover { background: var(--ev-accent); color: #fff !important; border-color: var(--ev-accent); }

                /* ============ SERVICES ============ */
                .ev-services { background: #fff; padding: 90px 0 60px; min-height: 100vh; display: flex; align-items: center; }
                .ev-services .ev-container { width: 100%; }
                .ev-services .ev-section-head { margin-bottom: 36px; }
                .ev-services .ev-section-head h2 { font-size: clamp(26px, 3.4vw, 34px); }
                .ev-services .ev-section-head p { font-size: 16px; margin-top: 10px; }
                .ev-services-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
                .ev-service-card { padding: 22px; border-radius: 16px; border: 1px solid var(--ev-border); background: #fff; transition: border-color .3s, box-shadow .3s; }
                .ev-service-card:hover { border-color: rgba(91,168,160,.3); box-shadow: 0 10px 25px -10px rgba(91,168,160,.12); }
                .ev-service-icon { width: 48px; height: 48px; border-radius: 12px; background: var(--ev-accent-light); color: var(--ev-accent); display: flex; align-items: center; justify-content: center; margin-bottom: 16px; transition: background-color .3s, color .3s; }
                .ev-service-card:hover .ev-service-icon { background: var(--ev-accent); color: #fff; }
                .ev-service-card h3 { font-size: 18px; margin-bottom: 8px; }
                .ev-service-card p { color: var(--ev-text-secondary); line-height: 1.5; font-size: 14px; }

                /* ============ KOHO PŘIJÍMÁME — psi / kočky / drobní savci (velké, viditelné) ============ */
                .ev-species { margin: 0 auto 44px; display: flex; flex-wrap: wrap; justify-content: center; gap: 14px; max-width: 760px; }
                .ev-species-chip { display: inline-flex; align-items: center; gap: 12px; background: var(--ev-accent); color: #fff; padding: 15px 30px; border-radius: 9999px; font-size: 19px; font-weight: 800; letter-spacing: .01em; box-shadow: 0 14px 28px -10px rgba(91,168,160,.6); }
                .ev-species-chip svg { width: 30px; height: 30px; flex-shrink: 0; }
                @media (max-width: 768px) {
                    /* tři stejně široké sloupečky vedle sebe, ikona nad názvem — vejde se i na úzký mobil */
                    .ev-species { margin: 6px auto 26px; gap: 8px; flex-wrap: nowrap; padding: 0 2px; max-width: 420px; }
                    .ev-species-chip { flex: 1 1 0; min-width: 0; flex-direction: column; gap: 6px; padding: 13px 6px; font-size: 13px; line-height: 1.15; text-align: center; border-radius: 18px; }
                    .ev-species-chip svg { width: 26px; height: 26px; }
                }

                .ev-services-mobile-list { display: none; }
                .ev-services-mobile-list .ev-srv-icon { width: 42px; height: 42px; border-radius: 12px; background: var(--ev-accent-light); color: var(--ev-accent); display: flex; align-items: center; justify-content: center; flex-shrink: 0; transition: background-color .2s, color .2s; }
                .ev-services-mobile-list .ev-srv-title { font-size: 16px; font-weight: 600; color: var(--ev-primary); flex: 1; text-align: left; }
                .ev-services-mobile-list .ev-srv-arrow { color: var(--ev-text-secondary); transition: transform .2s; flex-shrink: 0; }
                .ev-services-mobile-list .ev-srv-body { padding: 0 16px 16px 16px; color: var(--ev-text-secondary); font-size: 14px; line-height: 1.6; }
                .ev-services-mobile-list details { border: 1px solid var(--ev-border); border-radius: 14px; background: #fff; overflow: hidden; }
                .ev-services-mobile-list details[open] { box-shadow: 0 6px 18px -8px rgba(91,168,160,.22); border-color: rgba(91,168,160,.35); }
                .ev-services-mobile-list details[open] .ev-srv-icon { background: var(--ev-accent); color: #fff; }
                .ev-services-mobile-list details[open] .ev-srv-arrow { transform: rotate(180deg); color: var(--ev-accent); }
                .ev-services-mobile-list summary { list-style: none; cursor: pointer; display: flex; align-items: center; gap: 14px; padding: 12px 16px; }
                .ev-services-mobile-list summary::-webkit-details-marker { display: none; }

                /* ============ ABOUT ============ */
                .ev-about { background: var(--ev-muted); padding: 100px 0; min-height: 100vh; display: flex; align-items: center; }
                .ev-about .ev-container { width: 100%; }
                .ev-about-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: center; }
                .ev-about h2 { font-size: clamp(28px, 4vw, 38px); line-height: 1.15; }
                .ev-about-lead { margin-top: 24px; color: var(--ev-text-secondary); font-size: 18px; line-height: 1.65; }
                .ev-about-lead + .ev-about-lead { margin-top: 16px; }
                .ev-about-feats { margin-top: 32px; display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
                .ev-about-feat-item { display: flex; align-items: center; gap: 12px; }
                .ev-check-circle { width: 24px; height: 24px; border-radius: 9999px; background: var(--ev-accent-light); display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
                .ev-about-feat-item span { color: var(--ev-primary); font-weight: 500; }

                .ev-about-mission { display: flex; align-items: center; justify-content: center; }
                .ev-mission-card { position: relative; background: radial-gradient(circle at 100% 0%, rgba(91,168,160,.18) 0%, transparent 25%), radial-gradient(circle at 0% 100%, rgba(91,168,160,.12) 0%, transparent 20%), linear-gradient(135deg, var(--ev-primary), var(--ev-primary-dark)); border-radius: 28px; padding: 40px; color: #fff; overflow: visible; width: 100%; text-align: center; }
                .ev-mission-card > .ev-blob-1, .ev-mission-card > .ev-blob-2 { display: none !important; }
                .ev-mission-card > .ev-mission-icon { margin-left: auto !important; margin-right: auto !important; }
                .ev-mission-icon { position: relative; width: 64px; height: 64px; border-radius: 16px; background: rgba(255,255,255,.1); color: #fff; display: flex; align-items: center; justify-content: center; margin-bottom: 24px; }
                .ev-mission-card h3 { position: relative; font-size: 26px; color: #fff; margin-bottom: 16px; }
                .ev-mission-text { position: relative; color: rgba(255,255,255,.75); line-height: 1.65; margin-bottom: 28px; font-size: 16px; }
                .ev-mission-cta { position: relative; display: inline-flex; align-items: center; gap: 10px; background: var(--ev-accent); color: #fff; padding: 14px 24px; border-radius: 9999px; font-weight: 600; font-size: 15px; transition: background-color .2s; }
                .ev-mission-cta:hover { background: var(--ev-accent-dark); color: #fff; }

                /* ============ TESTIMONIALS ============ */
                .ev-testi { background: #fff; padding: 96px 0; }
                .ev-testi-track { display: flex; gap: 24px; overflow-x: auto; padding-bottom: 16px; scroll-snap-type: x mandatory; scrollbar-width: none; -ms-overflow-style: none; }
                .ev-testi-track::-webkit-scrollbar { display: none; }
                .ev-testi-card { min-width: 340px; max-width: 400px; flex-shrink: 0; scroll-snap-align: start; padding: 32px; border-radius: 18px; border: 1px solid var(--ev-border); background: rgba(247,249,251,.5); transition: box-shadow .2s; }
                .ev-testi-card:hover { box-shadow: 0 6px 16px -8px rgba(0,0,0,.08); }
                .ev-stars { display: flex; gap: 4px; }
                .ev-testi-text { margin-top: 16px; color: var(--ev-primary); font-size: 17px; line-height: 1.6; }
                .ev-testi-author { margin-top: 24px; display: flex; align-items: center; gap: 12px; }
                .ev-testi-avatar { width: 40px; height: 40px; border-radius: 9999px; background: var(--ev-accent-light); color: var(--ev-accent); display: flex; align-items: center; justify-content: center; font-weight: 700; font-size: 14px; }
                .ev-testi-name { font-weight: 600; color: var(--ev-primary); font-size: 14px; }
                .ev-testi-pet { color: var(--ev-text-secondary); font-size: 12px; margin-top: 2px; }
                .ev-google { margin-top: 48px; text-align: center; }
                .ev-google-pill { display: inline-flex; align-items: center; gap: 12px; background: var(--ev-muted); border-radius: 9999px; padding: 12px 24px; border: 1px solid var(--ev-border); font-size: 14px; color: var(--ev-text-secondary); }
                .ev-google-pill strong { color: var(--ev-primary); font-weight: 600; }

                /* ============ NÁŠ TÝM (teaser) ============ */
                .ev-teamcta { background: #fff; padding: 40px 0 96px; }
                .ev-teamcta-card { position: relative; background: radial-gradient(circle at 100% 0%, rgba(91,168,160,.20) 0%, transparent 30%), radial-gradient(circle at 0% 100%, rgba(91,168,160,.14) 0%, transparent 26%), linear-gradient(135deg, var(--ev-primary), var(--ev-primary-dark)); border-radius: 28px; padding: 56px 40px; color: #fff; overflow: hidden; display: flex; flex-direction: column; align-items: center; text-align: center; }
                .ev-teamcta-icon { position: relative; width: 64px; height: 64px; border-radius: 16px; background: rgba(255,255,255,.1); color: #fff; display: flex; align-items: center; justify-content: center; margin: 0 auto 20px auto; }
                .ev-teamcta-badge { position: relative; display: inline-flex; align-items: center; background: rgba(91,168,160,.2); color: #bfe6e1; padding: 6px 16px; border-radius: 9999px; font-size: 13px; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; margin-bottom: 16px; }
                .vet-page .ev-teamcta-card h2 { position: relative; font-size: clamp(24px, 3.4vw, 34px); line-height: 1.15; color: #fff; max-width: 640px; margin: 0 auto; text-align: center; }
                .ev-teamcta-text { position: relative; margin: 16px auto 0; max-width: 620px; color: rgba(255,255,255,.78); font-size: 17px; line-height: 1.65; text-align: center; }
                .ev-teamcta-btn { position: relative; margin-top: 30px; display: inline-flex; align-items: center; gap: 10px; background: var(--ev-accent); color: #fff !important; padding: 15px 30px; border-radius: 9999px; font-weight: 700; font-size: 15px; transition: background-color .2s, transform .1s; }
                .ev-teamcta-btn:hover { background: var(--ev-accent-dark); color: #fff !important; }
                .ev-teamcta-btn:active { transform: scale(.98); }
                .ev-teamcta-btn svg { flex-shrink: 0; }
                @media (max-width: 768px) {
                    .ev-teamcta { padding: 30px 0 70px; }
                    .ev-teamcta-card { padding: 44px 22px; border-radius: 24px; }
                    .ev-teamcta-icon { width: 58px; height: 58px; margin-bottom: 16px; }
                    .vet-page .ev-teamcta-card h2 { font-size: 24px; }
                    .ev-teamcta-text { font-size: 15.5px; margin-top: 14px; }
                    .ev-teamcta-btn { margin-top: 26px; width: 100%; max-width: 340px; justify-content: center; padding: 15px 24px; }
                }

                /* ============ CAREERS ============ */
                .ev-careers { background: #fff; padding: 96px 0; }
                .ev-careers-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: center; }
                .ev-careers h2 { font-size: clamp(28px, 4vw, 38px); line-height: 1.15; }
                .ev-careers-lead { margin-top: 24px; color: var(--ev-text-secondary); font-size: 18px; line-height: 1.65; }
                .ev-careers-list-title { font-size: 18px; font-weight: 600; color: var(--ev-primary); margin: 36px 0 22px; }
                .ev-careers-list { display: flex; flex-direction: column; gap: 18px; }
                .ev-careers-item { display: flex; align-items: center; gap: 14px; padding: 6px 0; }
                .ev-careers-bullet { width: 36px; height: 36px; border-radius: 9999px; background: var(--ev-accent-light); display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
                .ev-careers-item span { color: var(--ev-primary); font-weight: 500; font-size: 18px; }
                .ev-careers-cta { margin-top: 40px; display: flex; flex-wrap: wrap; gap: 14px; }

                .ev-careers-card { position: relative; background: linear-gradient(135deg, var(--ev-primary), var(--ev-primary-dark)); border-radius: 28px; padding: 40px; color: #fff; overflow: hidden; text-align: center; }
                .ev-careers-icon { position: relative; width: 64px; height: 64px; border-radius: 16px; background: rgba(255,255,255,.1); display: flex; align-items: center; justify-content: center; margin: 0 auto 24px auto; }
                .ev-careers-card h3 { font-size: 26px; color: #fff; margin-bottom: 16px; position: relative; }
                .ev-careers-card-text { color: rgba(255,255,255,.7); line-height: 1.6; margin-bottom: 32px; position: relative; }
                .ev-careers-benefits { display: inline-flex; flex-direction: column; gap: 14px; align-items: flex-start; position: relative; text-align: left; }
                .ev-careers-benefit { display: flex; align-items: center; gap: 12px; }
                .ev-careers-benefit-dot { width: 24px; height: 24px; border-radius: 9999px; background: rgba(91,168,160,.3); display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
                .ev-careers-benefit span { color: rgba(255,255,255,.9); font-weight: 500; }

                /* ============ CONTACT ============ */
                .ev-contact { background: var(--ev-muted); padding: 96px 0; }
                .ev-contact-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; }
                .ev-contact-bottom { display: contents; }
                /* ordinační hodiny jsou známé, takže tabulka jede i na PC — text „hodiny zveřejníme" už neplatí.
                   .ev-hours před třídou nadpisu je nutný: samotné .ev-hours-h3-desk prohraje s .ev-hours h3 */
                .ev-hours .ev-hours-h3-desk, .ev-hours-soft, .ev-call-label-mob { display: none; }
                .ev-contact-stack { display: flex; flex-direction: column; gap: 18px; }
                .ev-contact-card { position: relative; background: linear-gradient(180deg, #ffffff, #fbfdfd); border-radius: 20px; padding: 22px 24px; border: 1px solid var(--ev-border); overflow: hidden; transition: transform .25s ease, box-shadow .25s ease, border-color .25s ease; }
                .ev-contact-card::before { content: ""; position: absolute; left: 0; top: 0; bottom: 0; width: 4px; background: linear-gradient(180deg, var(--ev-accent), var(--ev-accent-dark)); transform: scaleY(0); transform-origin: top; transition: transform .3s ease; }
                .ev-contact-card:hover { transform: translateY(-3px); box-shadow: 0 20px 44px -20px rgba(17,27,48,.35); border-color: rgba(91,168,160,.45); }
                .ev-contact-card:hover::before { transform: scaleY(1); }
                .ev-contact-card-inner { position: relative; display: flex; align-items: center; gap: 16px; }
                .ev-contact-card-icon { width: 52px; height: 52px; border-radius: 16px; background: linear-gradient(140deg, var(--ev-accent), var(--ev-accent-dark)); color: #fff; display: flex; align-items: center; justify-content: center; flex-shrink: 0; box-shadow: 0 10px 22px -8px rgba(74,145,138,.7); transition: transform .25s ease; }
                .ev-contact-card:hover .ev-contact-card-icon { transform: scale(1.06) rotate(-3deg); }
                .ev-contact-card-body { flex: 1; min-width: 0; }
                .ev-contact-card h3 { font-weight: 700; color: var(--ev-primary); margin-bottom: 5px; font-size: 15px; letter-spacing: .01em; text-transform: uppercase; opacity: .7; }
                .ev-contact-card p { color: var(--ev-text-secondary); font-size: 14px; line-height: 1.6; }
                .ev-contact-role { margin-top: 5px; font-size: 12.5px; color: var(--ev-text-secondary); line-height: 1.45; }
                .ev-contact-link { color: var(--ev-primary); font-weight: 700; font-size: 17px; line-height: 1.4; display: inline-block; transition: color .2s; }
                .ev-contact-card:hover .ev-contact-link { color: var(--ev-accent-dark); }
                .ev-contact-link:hover { text-decoration: underline; }
                .ev-contact-arrow { flex-shrink: 0; color: var(--ev-border); transition: color .25s ease, transform .25s ease; }
                .ev-contact-card:hover .ev-contact-arrow { color: var(--ev-accent); transform: translateX(3px); }

                .ev-hours { background: #fff; border-radius: 18px; padding: 32px; border: 1px solid var(--ev-border); }
                .ev-hours h3 { font-size: 20px; font-weight: 600; color: var(--ev-primary); margin-bottom: 22px; padding-bottom: 16px; border-bottom: 1px solid var(--ev-border); display: flex; align-items: center; gap: 12px; }
                .ev-hours h3 svg { color: var(--ev-accent); }
                .ev-hours-soon-text { color: var(--ev-text-secondary); font-size: 15px; line-height: 1.6; margin-bottom: 16px; }
                .ev-hours-note { margin-top: 14px; color: var(--ev-text-secondary); font-size: 13px; line-height: 1.55; }
                .ev-hours-note strong { color: var(--ev-primary); font-weight: 700; }
                .ev-hours-emergency { color: #dc2626; font-weight: 800; cursor: pointer; text-decoration: underline; text-decoration-style: dotted; text-underline-offset: 3px; }
                .ev-hours-emergency:hover { text-decoration-style: solid; }
                .ev-hours-note + .ev-hours-note { margin-top: 8px; }
                .ev-hours-soon-text:last-of-type { margin-bottom: 0; }
                .ev-hours-row { display: flex; align-items: center; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid var(--ev-border); }
                .ev-hours-row:last-of-type { border-bottom: 0; }
                .ev-hours-day { color: var(--ev-primary); font-weight: 500; font-size: 14px; }
                .ev-hours-time { font-size: 14px; font-weight: 500; color: var(--ev-text-secondary); }
                .ev-hours-time.ev-closed { color: #ef4444; }
                .ev-hours-btn { margin-top: 28px; width: 100%; padding: 14px 24px; border-radius: 12px; background: var(--ev-accent); color: #fff; font-size: 14px; font-weight: 600; display: inline-flex; align-items: center; justify-content: center; gap: 8px; transition: background-color .2s; }
                .ev-hours-btn:hover { background: var(--ev-accent-dark); color: #fff; }

                .ev-map { display: block; background: #eef2f6; border-radius: 18px; border: 1px solid var(--ev-border); overflow: hidden; min-height: 320px; position: relative; scroll-margin-top: 88px; cursor: pointer; transition: box-shadow .25s ease; }
                .ev-map:hover { box-shadow: 0 18px 40px -18px rgba(17,27,48,.35); }
                /* statická „mapa" bez iframu (pure CSS) — jemná uliční mřížka + pin uprostřed, celé klikací na Google Maps */
                .ev-map-static { position: absolute; inset: 0;
                    background:
                        repeating-linear-gradient(0deg, transparent 0 38px, rgba(91,168,160,.10) 38px 40px),
                        repeating-linear-gradient(90deg, transparent 0 46px, rgba(91,168,160,.10) 46px 48px),
                        linear-gradient(135deg, #eef4f3, #e5edf2);
                }
                .ev-map-static::before { content: ""; position: absolute; left: 18%; top: 62%; width: 64%; height: 10px; background: rgba(91,168,160,.30); transform: rotate(-18deg); border-radius: 6px; }
                .ev-map-static::after { content: ""; position: absolute; left: 8%; top: 24%; width: 52%; height: 8px; background: rgba(26,39,68,.10); transform: rotate(12deg); border-radius: 6px; }
                .ev-map-pin { position: absolute; top: 44%; left: 50%; transform: translate(-50%, -50%); width: 56px; height: 56px; border-radius: 50%; background: var(--ev-accent); color: #fff; display: flex; align-items: center; justify-content: center; box-shadow: 0 14px 30px -10px rgba(91,168,160,.85); z-index: 1; }
                .ev-map-pin svg { width: 30px; height: 30px; }
                .ev-map-overlay { position: absolute; left: 14px; right: 14px; bottom: 14px; background: rgba(255,255,255,.96); -webkit-backdrop-filter: blur(8px); backdrop-filter: blur(8px); border-radius: 14px; padding: 14px 16px; text-align: center; box-shadow: 0 10px 24px -8px rgba(17,27,48,.22); border: 1px solid var(--ev-border); }
                .ev-map-overlay p { color: var(--ev-text-secondary); font-size: 13px; margin: 0; }
                .ev-map-overlay p.ev-map-addr { color: var(--ev-primary); font-weight: 700; font-size: 15px; margin-bottom: 2px; }
                .ev-map-link { display: inline-flex; align-items: center; gap: 6px; color: var(--ev-accent); font-size: 13px; font-weight: 600; margin-top: 8px; }
                .ev-map-link:hover { text-decoration: underline; }

                /* ============ RESPONSIVE ============ */
                @media (max-width: 1100px) {
                    /* o polozku v menu vic (Nas tym) — stahnout mezery, at se to do listy vejde */
                    .ev-topbar-nav { gap: 13px; }
                    /* textovy odkaz Kontakt duplikuje zeleny Kontakt button vedle — na uzkem PC ho schovame, jinak nav dosedne na logo */
                    .ev-topbar-nav > a[href$="#kontakt"] { display: none; }
                    .ev-topbar-nav a { font-size: 13.5px; white-space: nowrap; }
                    .ev-call--topbar > summary { padding: 9px 14px; }
                    .ev-services-grid { grid-template-columns: repeat(2, 1fr); }
                    .ev-contact-grid { grid-template-columns: 1fr; }
                }
                @media (max-width: 768px) {
                    .ev-container { padding: 0 20px; }
                    .ev-topbar { background: #fff; -webkit-backdrop-filter: none; backdrop-filter: none; }
                    html, body { overscroll-behavior-y: none; -webkit-overflow-scrolling: touch; }
                    .ev-feat-row { -webkit-backdrop-filter: none; backdrop-filter: none; background: rgba(255,255,255,.08); }
                    .ev-map-overlay { -webkit-backdrop-filter: none; backdrop-filter: none; background: #fff; }
                    .ev-burger-panel { -webkit-backdrop-filter: none; backdrop-filter: none; }
                    .ev-call-panel { -webkit-backdrop-filter: none; backdrop-filter: none; }
                    html { scroll-behavior: auto; }
                    .ev-topbar-inner { height: 60px; }
                    .ev-topbar-nav { gap: 10px; }
                    .ev-topbar-nav a:not(.ev-topbar-cta):not(.ev-burger-link):not(.ev-burger-tel):not(.ev-lang-opt) { display: none; }
                    .ev-topbar-nav > .ev-call--topbar { display: none; }
                    .ev-lang > summary { padding: 7px 11px; font-size: 12.5px; gap: 6px; }
                    .ev-lang-panel { min-width: 158px; }
                    .ev-burger { display: inline-flex; }
                    .ev-topbar-logo { font-size: 17px; }
                    .ev-topbar-img { height: 36px; }
                    .ev-call-panel { min-width: 230px; max-width: calc(100vw - 32px); }
                    /* Sekce .ev-hero na strance uz neni (nahradil ji letak), takze tohle
                       pravidlo s fotkou kliniky na pozadi nic nevykreslovalo. */
                    .ev-hero .ev-container { height: 100%; display: block; }
                    .ev-hero-grid { display: flex; flex-direction: column; grid-template-columns: 1fr; gap: 0; text-align: center; height: 100%; }
                    .ev-hero-grid > div:first-child { display: flex; flex-direction: column; height: 100%; }
                    .ev-hero-logo-mobile { display: block !important; margin: auto auto; max-width: 440px; width: 90vw; height: auto; flex-shrink: 0; visibility: hidden; }
                    .ev-hero .ev-badge { margin: 0 auto; padding: 7px 18px; font-size: 14px; flex-shrink: 0; }
                    .ev-hero h1 { display: none; }
                    .ev-hero-lead { margin: auto auto 0; font-size: 15.5px; line-height: 1.5; max-width: 380px; padding-top: 0; transform: translateY(35px); }
                    .ev-hero-cta { justify-content: center; align-items: center; margin-top: 14px; gap: 10px; transform: translateY(50px); }
                    .ev-hero-cta > .ev-call { width: 100%; max-width: 340px; }
                    .ev-hero-cta > .ev-call > summary { width: 100%; padding: 15px 24px; font-size: 15.5px; }
                    .ev-hero-cta > .ev-call > .ev-call-panel { top: auto; bottom: calc(100% + 10px); left: 0 !important; right: 0 !important; transform: none !important; width: 100%; min-width: 0; max-width: none; }
                    .ev-hero-cta > .ev-btn { padding: 14px 24px; font-size: 15.5px; width: 100%; max-width: 340px; }
                    .ev-hero-stats { justify-content: center; margin-top: 12px; gap: 18px; transform: translateY(50px); }
                    .ev-hero-stat-num { font-size: 22px; }
                    .ev-hero-stat-lbl { font-size: 12px; }
                    .ev-hero-card { display: none; }
                    .ev-about-grid { grid-template-columns: 1fr; gap: 40px; text-align: center; }
                    .ev-about-feats { grid-template-columns: 1fr; justify-items: center; }
                    .ev-about-feat-item { justify-content: center; }
                    .ev-about .ev-badge { margin-left: auto; margin-right: auto; }
                    .ev-careers-grid { grid-template-columns: 1fr; gap: 40px; text-align: center; }
                    .ev-careers .ev-badge { margin-left: auto; margin-right: auto; }
                    .ev-careers-list { display: inline-flex; flex-direction: column; align-items: flex-start; text-align: left; }
                    .ev-careers-cta { justify-content: center; }
                    .ev-services-grid { display: none; }
                    .ev-services-mobile-list { display: flex; flex-direction: column; gap: 8px; }
                    .ev-testi-card { min-width: 85vw; }

                    /* na mobilu je hero jen countdown — starou hero sekci schováme (na PC zůstává) */
                    .ev-hero { display: none !important; }

                    /* COUNTDOWN HERO — mobil */
                    /* obsah se rozprostře po celé výšce místo aby seděl jako blok uprostřed */
                    .ev-cd { --cd-pt: 68px; --cd-pb: 14px; align-items: stretch; background-position: center top, center, center, center top, center; }
                    .ev-cd-inner { justify-content: space-evenly; }
                    /* JEDNA velikost pro celý mobil — žádný výškový media query.
                       Ten se na iOS překlápěl, když Safari zatáhne lišty, a layout při scrollu skákal. */
                    .ev-cd-badge { font-size: 15.5px; padding: 9px 20px; gap: 8px; margin-top: 12px; letter-spacing: .05em; }
                    .ev-cd-badge svg { width: 18px; height: 18px; }
                    .ev-cd-emergency { font-size: 10.5px; padding: 6px 14px; gap: 7px; letter-spacing: .02em; }
                    .ev-cd-emergency svg { width: 14px; height: 14px; }
                    .ev-cd-emergency-dot { width: 7px; height: 7px; }
                    .vet-page .ev-cd-title { margin-top: 10px; font-size: 22px; }
                    .ev-cd-date { margin-top: 8px; font-size: 16px; padding: 8px 16px; gap: 8px; }
                    .ev-cd-date svg { width: 16px; height: 16px; }
                    .ev-cd-clock { margin-top: 12px; gap: 6px; }
                    .ev-cd-unit { padding: 8px 3px 7px; border-radius: 13px; background: var(--ev-muted); }
                    .ev-cd-num { font-size: 10.5vw; letter-spacing: -0.05em; }
                    .ev-cd-lbl { margin-top: 4px; font-size: 8.5px; letter-spacing: .05em; }
                    .ev-cd-note { margin-top: 7px; gap: 6px; }
                    .ev-cd-note-live { font-size: 11px; }
                    .ev-cd-addr { padding: 7px 15px; font-size: 12.5px; }
                    .ev-cd-addr svg { width: 14px; height: 14px; }
                    .ev-cd-text { margin-top: 10px; }
                    .ev-cd-text p { font-size: 12px; line-height: 1.45; }
                    .ev-cd-text p + p { margin-top: 8px; }
                    .ev-cd-social { margin-top: 22px; gap: 10px; }
                    .ev-cd-soc { width: 44px; height: 44px; }
                    .ev-cd-soc svg { width: 21px; height: 21px; }
                    .ev-cd-cta { margin-top: 16px; flex-direction: column; width: 100%; max-width: 330px; gap: 7px; }
                    .ev-cd-cta > .ev-cd-btn { width: 100%; padding: 10px 18px; font-size: 14px; }
                    .ev-cd-cta > .ev-call { width: 100%; }
                    .ev-cd-cta > .ev-call > summary { width: 100%; padding: 10px 18px; font-size: 14px; }
                    .ev-cd-cta > .ev-call > .ev-call-panel { top: auto; bottom: calc(100% + 10px); left: 0 !important; right: 0 !important; transform: none !important; width: 100%; min-width: 0; max-width: none; }

                    /* === ALL SECTIONS AS 100vh SLIDES === */
                    .ev-services, .ev-about, .ev-careers, .ev-contact {
                        min-height: 100vh;
                        min-height: 100svh;
                        display: flex; align-items: stretch; justify-content: center;
                        padding: 76px 0 40px; overflow: hidden;
                    }
                    .ev-services .ev-container,
                    .ev-about .ev-container,
                    .ev-careers .ev-container,
                    .ev-contact .ev-container { width: 100%; display: flex; flex-direction: column; justify-content: center; }
                    .ev-section-head { margin-bottom: 28px; flex-shrink: 0; }
                    .ev-section-head h2 { font-size: 24px; line-height: 1.2; }
                    .ev-section-head p { font-size: 14px; margin-top: 8px; }
                    .ev-section-head .ev-badge { margin-bottom: 10px; padding: 5px 14px; font-size: 12.5px; }

                    /* SERVICES */
                    .ev-services { padding: 80px 0 40px; }
                    .ev-services .ev-container { display: flex; flex-direction: column; justify-content: space-evenly; height: 100%; }
                    .ev-services .ev-section-head { margin-bottom: 0; flex-shrink: 0; }
                    .ev-services .ev-section-head h2 { font-size: 26px; line-height: 1.2; }
                    .ev-services .ev-section-head p { display: none; }
                    .ev-services .ev-section-head .ev-badge { margin-bottom: 12px; padding: 6px 16px; font-size: 13px; }
                    .ev-services-mobile-list { gap: 10px; flex-shrink: 0; }
                    .ev-services-mobile-list summary { padding: 14px 18px; gap: 14px; }
                    .ev-services-mobile-list .ev-srv-icon { width: 44px; height: 44px; border-radius: 14px; }
                    .ev-services-mobile-list .ev-srv-icon svg { width: 22px; height: 22px; }
                    .ev-services-mobile-list .ev-srv-title { font-size: 16px; }
                    .ev-services-mobile-list .ev-srv-arrow { width: 18px; height: 18px; }
                    .ev-services-mobile-list .ev-srv-body { font-size: 14px; padding: 0 16px 14px; line-height: 1.55; }
                    .ev-services-mobile-list details { border-radius: 14px; }

                    /* ABOUT — two stacked 100vh slides on mobile */
                    .ev-about { padding: 0; min-height: 0; overflow: visible; display: block; }
                    .ev-about > .ev-container { padding: 0; display: block; max-width: none; }
                    .ev-about-grid { display: flex; flex-direction: column; gap: 0; grid-template-columns: 1fr; align-items: stretch; }
                    .ev-about-grid > div:first-child { min-height: 100vh; min-height: 100svh; display: flex; flex-direction: column; justify-content: space-evenly; padding: 80px 20px 40px; text-align: center; }
                    .ev-about-grid > .ev-about-mission { min-height: 100vh; min-height: 100svh; display: flex; align-items: center; justify-content: center; padding: 80px 20px 40px; background: radial-gradient(circle at 100% 0%, rgba(91,168,160,.18) 0%, transparent 25%), radial-gradient(circle at 0% 100%, rgba(91,168,160,.12) 0%, transparent 20%), linear-gradient(135deg, var(--ev-primary), var(--ev-primary-dark)); }
                    /* panel „Otevíráme brzy" na mobilu pryč — odpočet v heru už termín říká (na PC zůstává) */
                    .ev-about-grid > .ev-about-mission { display: none !important; }
                    .ev-about .ev-badge { margin: 0 auto 0 auto; padding: 7px 18px; font-size: 13.5px; flex-shrink: 0; }
                    .ev-about h2 { font-size: 26px; line-height: 1.2; margin-bottom: 0; flex-shrink: 0; }
                    .ev-about-lead { font-size: 15.5px; line-height: 1.6; margin-top: 0; }
                    .ev-about-lead + .ev-about-lead { margin-top: 14px; }
                    .ev-about-feats { margin-top: 0; gap: 18px 24px; grid-template-columns: 1fr 1fr; max-width: 380px; margin-left: auto; margin-right: auto; flex-shrink: 0; }
                    .ev-about-feat-item { gap: 12px; }
                    .ev-about-feat-item span { font-size: 15px; line-height: 1.3; }
                    .ev-check-circle { width: 30px; height: 30px; flex-shrink: 0; }
                    .ev-check-circle svg { width: 18px; height: 18px; }
                    .ev-mission-card { padding: 0; border-radius: 0; position: relative; z-index: 5; overflow: visible; max-width: 480px; width: 100%; background: transparent; }
                    .ev-mission-card h3 { font-size: 28px; margin-bottom: 18px; }
                    .ev-mission-icon { width: 68px; height: 68px; margin-bottom: 22px; }
                    .ev-mission-icon svg { width: 36px; height: 36px; }
                    .ev-mission-text { font-size: 16.5px; line-height: 1.65; margin-bottom: 28px; }
                    .ev-call--mission > summary, a.ev-call--mission { padding: 15px 28px !important; font-size: 16px !important; }
                    .ev-mission-card .ev-call--mission > .ev-call-panel { top: auto; bottom: calc(100% + 10px); left: 0 !important; right: 0 !important; transform: none !important; width: 100%; min-width: 0; max-width: none; }

                    /* CAREERS — two stacked 100vh slides on mobile */
                    .ev-careers { padding: 0; min-height: 0; overflow: visible; display: block; background: #fff; }
                    .ev-careers > .ev-container { padding: 0; display: block; max-width: none; }
                    .ev-careers-grid { display: flex; flex-direction: column; gap: 0; grid-template-columns: 1fr; align-items: stretch; }
                    .ev-careers-grid > div:first-child { min-height: 100vh; min-height: 100svh; display: flex; flex-direction: column; justify-content: space-evenly; padding: 80px 20px 40px; text-align: center; background: #fff; }
                    .ev-careers-grid > div:last-child { min-height: 100vh; min-height: 100svh; display: flex; align-items: center; justify-content: center; padding: 80px 20px 40px; background: radial-gradient(circle at 100% 0%, rgba(91,168,160,.18) 0%, transparent 25%), radial-gradient(circle at 0% 100%, rgba(91,168,160,.12) 0%, transparent 20%), linear-gradient(135deg, var(--ev-primary), var(--ev-primary-dark)); }
                    .ev-careers .ev-badge { margin-bottom: 0; padding: 7px 18px; font-size: 13.5px; flex-shrink: 0; }
                    .ev-careers h2 { font-size: 26px; line-height: 1.2; margin-bottom: 0; flex-shrink: 0; }
                    .ev-careers-lead { font-size: 15.5px; line-height: 1.6; margin-top: 0; }
                    .ev-careers-list-title { font-size: 17px; margin: 0; }
                    .ev-careers-list { gap: 14px; flex-shrink: 0; }
                    .ev-careers-item { padding: 0; gap: 14px; }
                    .ev-careers-bullet { width: 38px; height: 38px; }
                    .ev-careers-item span { font-size: 16px; }
                    .ev-careers-cta { margin-top: 0; gap: 12px; flex-direction: column; width: 100%; max-width: 340px; margin-left: auto; margin-right: auto; flex-shrink: 0; }
                    .ev-careers-cta > .ev-btn, .ev-careers-cta > .ev-call { width: 100%; }
                    .ev-careers-cta > .ev-btn { padding: 15px 24px; font-size: 15.5px; }
                    .ev-careers-cta > .ev-call > summary, .ev-careers-cta > a.ev-call { width: 100%; padding: 15px 24px; font-size: 15.5px; }
                    .ev-careers-cta > .ev-call > .ev-call-panel { top: auto; bottom: calc(100% + 10px); left: 0 !important; right: 0 !important; transform: none !important; width: 100%; min-width: 0; max-width: none; }
                    .ev-careers-card { padding: 0; border-radius: 0; background: transparent; max-width: 480px; width: 100%; overflow: visible; }
                    .ev-careers-card > .ev-blob-1, .ev-careers-card > .ev-blob-2 { display: none !important; }
                    .ev-careers-card h3 { font-size: 28px; margin-bottom: 18px; }
                    .ev-careers-icon { width: 68px; height: 68px; margin-bottom: 22px; }
                    .ev-careers-icon svg { width: 36px; height: 36px; }
                    .ev-careers-card-text { font-size: 16.5px; line-height: 1.65; margin-bottom: 28px; }
                    .ev-careers-benefits { gap: 16px; }
                    .ev-careers-benefit span { font-size: 15.5px; }

                    /* CONTACT — slide 1: head + cards, slide 2: hours + map together */
                    .ev-contact { padding: 0; min-height: 0; overflow: visible; display: block; background: var(--ev-muted); }
                    .ev-contact > .ev-container { padding: 0; display: block; max-width: none; width: 100%; }
                    .ev-contact .ev-section-head { margin: 0; padding: 80px 20px 0; max-width: none; }
                    .ev-contact .ev-section-head .ev-badge { margin-bottom: 12px; padding: 7px 18px; font-size: 13.5px; }
                    .ev-contact .ev-section-head h2 { font-size: 28px; line-height: 1.2; }
                    .ev-contact .ev-section-head p { font-size: 15px; margin-top: 10px; }
                    .ev-contact-grid { display: flex; flex-direction: column; gap: 0; grid-template-columns: 1fr; }
                    .ev-contact-grid > .ev-contact-stack { min-height: calc(100vh - 220px); min-height: calc(100svh - 220px); display: flex; flex-direction: column; justify-content: space-evenly; gap: 0; padding: 30px 20px 40px; }
                    .ev-contact-grid > .ev-contact-bottom { min-height: 100vh; min-height: 100svh; display: flex; flex-direction: column; justify-content: center; gap: 14px; padding: 70px 16px 30px; }
                    .ev-contact-bottom > .ev-hours { padding: 18px 20px; border-radius: 18px; flex-shrink: 0; }
                    .ev-hours-h3-desk, .ev-hours-soft, .ev-call-label-desk { display: none !important; }
                    .ev-hours-h3-mob, .ev-call-label-mob { display: inline-flex !important; align-items: center; gap: 8px; }
                    .ev-hours-table { display: block !important; }
                    .ev-contact-bottom > .ev-map { flex: 1 1 auto; min-height: 180px; border-radius: 16px; }
                    .ev-contact-card { padding: 26px; border-radius: 18px; }
                    .ev-contact-card-inner { gap: 18px; align-items: center; }
                    .ev-contact-card-icon { width: 58px; height: 58px; border-radius: 16px; }
                    .ev-contact-card-icon svg { width: 28px; height: 28px; }
                    .ev-contact-card h3 { font-size: 18px; margin-bottom: 8px; }
                    .ev-contact-card p, .ev-contact-link { font-size: 15.5px; line-height: 1.5; }
                    .ev-hours h3 { font-size: 18px; margin-bottom: 12px; padding-bottom: 12px; gap: 10px; }
                    .ev-hours h3 svg { width: 20px; height: 20px; }
                    .ev-hours-row { padding: 7px 0; }
                    .ev-hours-day { font-size: 14.5px; }
                    .ev-hours-time { font-size: 14.5px; }
                    .ev-hours .ev-call--full { margin-top: 16px !important; }
                    .ev-hours .ev-call--full > summary, .ev-hours a.ev-call--full { padding: 13px 22px; font-size: 14.5px; }
                    .ev-hours .ev-call--full > .ev-call-panel { top: auto; bottom: calc(100% + 10px); left: 0 !important; right: 0 !important; transform: none !important; width: 100%; min-width: 0; max-width: none; }
                    .ev-map-overlay { left: 12px; right: 12px; bottom: 12px; padding: 9px 12px; border-radius: 12px; }
                    .ev-map-overlay p.ev-map-addr { font-size: 13px; margin-bottom: 1px; }
                    .ev-map-overlay p { font-size: 11.5px; }
                    .ev-map-link { font-size: 12px; margin-top: 4px; gap: 4px; }
                }
                @media (max-width: 480px) {
                    .ev-container { padding: 0 16px; }
                    .ev-hero { padding: 80px 16px 48px; height: 100vh; height: 100svh; min-height: 540px; }
                    .ev-hero h1 { display: none; }
                    .ev-hero-lead { font-size: 14px; margin-top: 14px; }
                    .ev-hero-cta { flex-direction: column; gap: 10px; width: 100%; max-width: 320px; margin-left: auto; margin-right: auto; }
                    .ev-hero-cta .ev-btn { width: 100%; padding: 12px 18px; font-size: 14px; }
                    .ev-hero-stats { gap: 16px; flex-wrap: wrap; margin-top: 22px; }
                    .ev-hero-stat-num { font-size: 22px; }
                    .ev-hero-stat-lbl { font-size: 12px; }
                    .ev-hero-divider { display: none; }
                    .ev-services-grid { grid-template-columns: 1fr; }
                    .ev-btn { padding: 12px 22px; font-size: 14px; }
                    .ev-feat-row { padding: 12px; }
                }

                /* COUNTDOWN HERO — nízké desktopy a notebooky (1280×720, 1366×768), ať se vejde bez scrollu */
                @media (min-width: 769px) and (max-height: 1000px) {
                    .ev-cd { --cd-pt: 76px; --cd-pb: 24px; }
                    .ev-flyer img { max-height: 60vh; }
                    .ev-cd-count-label { margin-top: 16px; }
                    .vet-page .ev-cd-title { margin-top: 14px; font-size: clamp(26px, 3.4vw, 40px); }
                    .ev-cd-date { margin-top: 10px; font-size: 22px; padding: 10px 22px; }
                    .ev-cd-date svg { width: 18px; height: 18px; }
                    .ev-cd-clock { margin-top: 21px; gap: 12px; max-width: 780px; }
                    .ev-cd-unit { padding: 16px 10px 12px; border-radius: 18px; }
                    .ev-cd-num { font-size: clamp(40px, 9vh, 78px); }
                    .ev-cd-lbl { margin-top: 8px; font-size: 11px; letter-spacing: .14em; }
                    .ev-cd-note { margin-top: 14px; }
                    .ev-cd-text { margin-top: 20px; max-width: 720px; }
                    .ev-cd-text p { font-size: 15px; line-height: 1.6; }
                    .ev-cd-text p + p { margin-top: 10px; }
                    .ev-cd-cta { margin-top: 14px; }
                    .ev-cd-cta > .ev-cd-btn { padding: 12px 22px; font-size: 14px; }
                    .ev-cd-cta > .ev-call > summary { padding: 12px 22px; font-size: 14px; }
                    .ev-cd-social { margin-top: 20px; gap: 11px; }
                    .ev-cd-soc { width: 48px; height: 48px; }
                    .ev-cd-soc svg { width: 23px; height: 23px; }
                }

                /* ============ SR-ONLY (skryté pro oči, čitelné pro Google/čtečky — nese hlavní SEO klíčové slovo v H1) ============ */
                .ev-sr-only { position: absolute !important; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0; }

                /* ============ AUTORSKÝ KREDIT (patička) — menší, ale výrazný ============ */
                .ev-footer-credit { padding: 18px 24px 20px; text-align: center; font-size: 13px; font-weight: 700; letter-spacing: 0.02em; color: #fff; background: linear-gradient(135deg, var(--ev-primary), var(--ev-primary-dark)); border-top: 3px solid var(--ev-accent); }
                .ev-footer-credit a { color: var(--ev-accent); font-weight: 800; border-bottom: 1.5px solid var(--ev-accent); padding-bottom: 1px; transition: color 0.25s, border-color 0.25s; }
                .ev-footer-credit a:hover { color: #fff; border-color: #fff; }

                /* ============ ODOO EDITOR / PŘEKLAD ============ */
                /* splash je fixed přes celou obrazovku se z-indexem 9999 — v editoru překrývá obsah a nejde klikat do textu */
                body.editor_enable .ev-splash,
                body.editor_has_snippets .ev-splash,
                body.o_translate_mode .ev-splash { display: none !important; }
                /* zavřené details (rozbalovací sekce) skrývají text před editorem i před režimem překladu — v editoru je otevřeme.
                   Popupy (.ev-call, .ev-burger, .ev-lang) necháme zavřené, jinak by plavaly přes obsah. */
                body.editor_enable details:not(.ev-call):not(.ev-burger):not(.ev-lang) > *:not(summary),
                body.editor_has_snippets details:not(.ev-call):not(.ev-burger):not(.ev-lang) > *:not(summary),
                body.o_translate_mode details:not(.ev-call):not(.ev-burger):not(.ev-lang) > *:not(summary) { display: block !important; }
                body.editor_enable details:not(.ev-call):not(.ev-burger):not(.ev-lang)::details-content,
                body.editor_has_snippets details:not(.ev-call):not(.ev-burger):not(.ev-lang)::details-content,
                body.o_translate_mode details:not(.ev-call):not(.ev-burger):not(.ev-lang)::details-content { content-visibility: visible !important; }

                /* ============ REZERVACE (vlozeny rezervacni system) ============ */
                .ev-rez { background: var(--ev-muted); padding: 88px 0; }
                .ev-rez-head { text-align: center; max-width: 640px; margin: 0 auto 36px auto; }
                .ev-rez-badge { display: inline-flex; align-items: center; background: rgba(43,120,112,.12); color: var(--ev-accent-dark); padding: 6px 16px; border-radius: 9999px; font-size: 13px; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; margin-bottom: 16px; }
                .vet-page .ev-rez-head h2 { font-size: clamp(24px, 3.4vw, 34px); line-height: 1.15; color: var(--ev-primary); margin: 0 0 12px 0; }
                .ev-rez-sub { color: var(--ev-text-secondary); font-size: 16px; margin: 0; }
                .ev-rez-frame { position: relative; background: #fff; border: 1px solid var(--ev-border); border-radius: 20px; overflow: hidden; box-shadow: 0 18px 48px rgba(26,39,68,.10); }
                .ev-rez-frame iframe { display: block; width: 100%; height: 900px; border: 0; }
                .ev-rez-fallback { text-align: center; margin-top: 20px; }
                .ev-rez-fallback a { display: inline-flex; align-items: center; gap: 8px; color: var(--ev-accent-dark); font-weight: 600; font-size: 15px; }
                @media (max-width: 767px) {
                    .ev-rez { padding: 56px 0; }
                    .ev-rez-frame iframe { height: 700px; }
                }

                /* ============ REZERVACNI STRANKA — HERO ============ */
                .ev-rezhero { padding: 128px 0 48px; background: linear-gradient(180deg, #ffffff 0%, var(--ev-muted) 100%); text-align: center; }
                .ev-rezhero .ev-container { max-width: 780px; }
                .ev-rezhero-badge { display: inline-flex; align-items: center; gap: 8px; background: var(--ev-accent-light); color: var(--ev-accent-dark); padding: 7px 18px; border-radius: 9999px; font-size: 13px; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; }
                .ev-rezhero-badge svg { flex-shrink: 0; }
                .vet-page .ev-rezhero h1 { margin-top: 18px; font-size: clamp(30px, 4.2vw, 44px); line-height: 1.1; color: var(--ev-primary); }
                .ev-rezhero-lead { margin-top: 14px; color: var(--ev-text-secondary); font-size: 17px; line-height: 1.6; }
                .ev-rezhero-meta { margin-top: 24px; display: flex; flex-wrap: wrap; justify-content: center; gap: 10px; }
                .ev-rezhero-chip { display: inline-flex; align-items: center; gap: 8px; background: #fff; border: 1px solid var(--ev-border); border-radius: 9999px; padding: 9px 16px; font-size: 14px; font-weight: 600; color: var(--ev-primary); }
                .ev-rezhero-chip:hover { color: var(--ev-primary); border-color: rgba(43,120,112,.4); }
                .ev-rezhero-chip svg { color: var(--ev-accent); flex-shrink: 0; }

                /* misto, kam se v editoru vlozi blok s rezervacnim systemem */
                .ev-rezslot { background: var(--ev-muted); min-height: 8px; }

                /* ============ FAQ ============ */
                .ev-faq { background: #fff; padding: 80px 0 96px; }
                .ev-faq .ev-container { max-width: 860px; }
                .ev-faq-head { text-align: center; margin-bottom: 36px; }
                .vet-page .ev-faq-head h2 { font-size: clamp(26px, 3.4vw, 34px); line-height: 1.15; }
                .ev-faq-head p { margin-top: 12px; color: var(--ev-text-secondary); font-size: 16px; }
                .ev-faq-list { display: flex; flex-direction: column; gap: 10px; }
                .ev-faq-item { border: 1px solid var(--ev-border); border-radius: 14px; background: #fff; overflow: hidden; }
                .ev-faq-item[open] { border-color: rgba(43,120,112,.35); box-shadow: 0 6px 18px -8px rgba(43,120,112,.22); }
                .ev-faq-item > summary { list-style: none; cursor: pointer; display: flex; align-items: center; gap: 14px; padding: 16px 18px; font-size: 16px; font-weight: 600; color: var(--ev-primary); text-align: left; }
                .ev-faq-item > summary::-webkit-details-marker { display: none; }
                .ev-faq-item > summary::marker { content: ""; }
                .ev-faq-item > summary:hover { background: var(--ev-muted); }
                .ev-faq-q { flex: 1; }
                .ev-faq-arrow { flex-shrink: 0; color: var(--ev-text-secondary); transition: transform .2s; }
                .ev-faq-item[open] .ev-faq-arrow { transform: rotate(180deg); color: var(--ev-accent); }
                .ev-faq-a { padding: 0 18px 18px 18px; color: var(--ev-text-secondary); font-size: 15px; line-height: 1.65; text-align: left; }
                .ev-faq-a a { color: var(--ev-accent-dark); font-weight: 600; }
                .ev-faq-a strong { color: var(--ev-primary); font-weight: 700; }
                @media (max-width: 768px) {
                    .ev-rezhero { padding: 104px 0 36px; }
                    .ev-rezhero-lead { font-size: 15.5px; }
                    .ev-faq { padding: 56px 0 72px; }
                    .ev-faq-head { margin-bottom: 26px; }
                    .ev-faq-item > summary { padding: 14px 16px; font-size: 15px; gap: 12px; }
                    .ev-faq-a { padding: 0 16px 16px 16px; font-size: 14.5px; }
                }

                /* ============ VLOZENY REZERVACNI SYSTEM (WinVet) ============ */
                .evrez{background:#f7f9fb;padding:0 0 44px 0;text-align:center;container-type:inline-size}
                .evrez .evrez-frame{display:inline-block;max-width:100%;width:1150px;background:#fff;border:1px solid #e2e8f0;border-radius:20px;overflow:hidden;box-shadow:0 18px 48px rgba(26,39,68,.10)}
                .evrez .evrez-inner{width:100%}
                .evrez .evrez-inner iframe{display:block;width:100%;height:780px;border:0}
                @container (max-width:900px){
                  .evrez .evrez-frame{border-radius:14px;box-shadow:none}
                  .evrez .evrez-inner{width:450px;transform-origin:top left}
                  .evrez .evrez-inner iframe{width:450px;height:820px}
                }
                @container (max-width:290px){ .evrez .evrez-inner{transform:scale(.62)} .evrez .evrez-frame{width:279px;height:508px} }
                @container (min-width:291px) and (max-width:320px){ .evrez .evrez-inner{transform:scale(.64)} .evrez .evrez-frame{width:288px;height:525px} }
                @container (min-width:321px) and (max-width:350px){ .evrez .evrez-inner{transform:scale(.71)} .evrez .evrez-frame{width:320px;height:582px} }
                @container (min-width:351px) and (max-width:380px){ .evrez .evrez-inner{transform:scale(.78)} .evrez .evrez-frame{width:351px;height:640px} }
                @container (min-width:381px) and (max-width:410px){ .evrez .evrez-inner{transform:scale(.84)} .evrez .evrez-frame{width:378px;height:689px} }
                @container (min-width:411px) and (max-width:440px){ .evrez .evrez-inner{transform:scale(.91)} .evrez .evrez-frame{width:410px;height:746px} }
                @container (min-width:441px) and (max-width:470px){ .evrez .evrez-inner{transform:scale(.97)} .evrez .evrez-frame{width:437px;height:795px} }
                @container (min-width:471px) and (max-width:900px){ .evrez .evrez-inner{transform:scale(1)} .evrez .evrez-frame{width:450px;height:820px} }
                /* ====== NACITANI REZERVACE ======
                   Kolecko lezi POD iframem, ne pres nej. Dokud se WinVet nenacte,
                   je iframe pruhledny a kolecko je skrz nej videt; jakmile se
                   vykresli, prekryje ho vlastnim pozadim a kolecko zmizi samo.
                   Zadny skript, zadne hlidani udalosti.
                   Nahore, ne uprostred: ramecek je vysoky pres 780 px, takze
                   vycentrovane kolecko by skoncilo hluboko pod ohybem. */
                .evrez .evrez-inner{position:relative}
                .evrez .evrez-nacitani{position:absolute;inset:0;z-index:0;display:flex;flex-direction:column;align-items:center;justify-content:flex-start;padding-top:90px;gap:22px;color:#1a2744;font-size:19px;font-weight:700;letter-spacing:.01em;text-align:center}
                .evrez .evrez-inner iframe{position:relative;z-index:1;background:transparent}
                .evrez-spinner{width:58px;height:58px;border-radius:50%;border:5px solid #e2e8f0;border-top-color:#2b7870;animation:evrezToci .85s linear infinite}
                .evrez-nacitani-podtext{font-size:14px;font-weight:500;color:#64748b;margin-top:-10px}
                /* Treti hlaska zabere zbytek vysky ramecku a vycentruje se v nem. */
                /* Text pod rezervaci — zustava videt i po nacteni WinVetu. */
                /* Behem nacitani sedi uprostred prazdneho ramecku, pak klesne pod nej. */
                .evrez-zprava--nacitani{flex:1 1 auto;min-height:0;display:flex;align-items:center;justify-content:center;max-width:520px;margin:0;padding:0 24px 56px;font-size:15px;color:#64748b}
                .evrez-zprava{max-width:680px;margin:26px auto 0;padding:0 24px;text-align:center;font-size:15.5px;line-height:1.7;color:#64748b;white-space:pre-line}
                @keyframes evrezToci{to{transform:rotate(360deg)}}
                @media (prefers-reduced-motion:reduce){.evrez-spinner{animation:none}}
                @media (max-width:900px){
                  .evrez .evrez-nacitani{padding-top:64px;gap:18px;font-size:17px}
                  .evrez-spinner{width:50px;height:50px;border-width:4px}
                  .evrez-nacitani-podtext{font-size:13px}
                  .evrez-zprava{margin-top:20px;font-size:14.5px;padding:0 18px}
                }

                /* ====== ODKAZ NA ROZPIS SLUZEB pod rezervaci ====== */
                .evrez-rozpis{background:#f7f9fb;padding:0 20px 62px;text-align:center}
                .evrez-rozpis-lead{margin:0 0 16px;color:#64748b;font-size:16px;line-height:1.55}
                .evrez-rozpis-btn{display:inline-flex;align-items:center;justify-content:center;gap:10px;min-width:260px;padding:15px 30px;border-radius:9999px;background:#fff;color:#2b7870 !important;border:1.5px solid #2b7870;font-size:15.5px;font-weight:700;text-decoration:none;transition:background-color .2s,color .2s,transform .1s}
                .evrez-rozpis-btn:hover{background:#2b7870;color:#fff !important}
                .evrez-rozpis-btn:active{transform:scale(.985)}
                .evrez-rozpis-btn svg{flex-shrink:0}
                @media (max-width:768px){
                  .evrez-rozpis{padding:0 20px 46px}
                  .evrez-rozpis-lead{font-size:15px}
                  .evrez-rozpis-btn{width:100%;max-width:340px;font-size:15px}
                }
            </style>

            <div id="wrap" class="oe_structure vet-page">

                <!-- =========================== TOPBAR =========================== -->
                <div class="ev-topbar">
                    <div class="ev-container ev-topbar-inner">
                        <a href="#odpocet" class="ev-topbar-logo">
                            <img src="/elite_vet_web/static/src/img/logo.jpeg" alt="Elite Vet" class="ev-topbar-img" loading="lazy"/>
                            <span class="ev-logo-brand" style="display: inline-block; white-space: nowrap;"><span style="color: #1a2744 !important;">Elite</span><span style="color: #2b7870 !important;">Vet</span></span>
                        </a>
                        <nav class="ev-topbar-nav">
                            <!-- Polozky navigace se berou z menu webu (Odoo: Web -> Upravit -> Menu),
                                 takze se daji menit bez zasahu do kodu stranky. -->
                            <t t-foreach="website.menu_id.child_id" t-as="ev_m">
                                <a t-att-href="ev_m.url" t-attf-class="#{'ev-nav-active' if ev_m.url == request.httprequest.path else ''}" t-att-target="'_blank' if '://' in ev_m.url else False" t-att-rel="'noopener' if '://' in ev_m.url else False"><span t-field="ev_m.name"/></a>
                            </t>
                            <details class="ev-lang">
                                <summary>
                                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">
                                        <circle cx="12" cy="12" r="10"/>
                                        <path d="M2 12h20"/>
                                        <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
                                    </svg>
                                    <span class="ev-lang-code ev-lang-code-cs">CZ</span><span class="ev-lang-code ev-lang-code-de">DE</span><span class="ev-lang-code ev-lang-code-en">EN</span><span class="ev-lang-code ev-lang-code-ru">RU</span>
                                    <svg class="ev-lang-caret" width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
                                        <polyline points="6 9 12 15 18 9"/>
                                    </svg>
                                </summary>
                                <div class="ev-lang-panel">
                                    <a data-no-post-process="1" t-att-href="ev_jazyk_cs" t-attf-class="ev-lang-opt{{ ' is-active' if request.env.lang == 'cs_CZ' else '' }}">Čeština</a>
                                    <a data-no-post-process="1" t-att-href="ev_jazyk_de" t-attf-class="ev-lang-opt{{ ' is-active' if request.env.lang == 'de_DE' else '' }}">Deutsch</a>
                                    <a data-no-post-process="1" t-att-href="ev_jazyk_en" t-attf-class="ev-lang-opt{{ ' is-active' if request.env.lang == 'en_US' else '' }}">English</a>
                                    <a data-no-post-process="1" t-att-href="ev_jazyk_ru" t-attf-class="ev-lang-opt{{ ' is-active' if request.env.lang == 'ru_RU' else '' }}">Русский</a>
                                </div>
                            </details>
                            <a t-attf-href="tel:#{ev_web.phone or '+420722599699'}" class="ev-call ev-call--topbar">
                                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/>
                                </svg>
                                Contact<br/>
                            </a>
                            <details class="ev-burger">
                                <summary aria-label="Menu">
                                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                                        <line x1="3" y1="6" x2="21" y2="6"/>
                                        <line x1="3" y1="12" x2="21" y2="12"/>
                                        <line x1="3" y1="18" x2="21" y2="18"/>
                                    </svg>
                                </summary>
                                <div class="ev-burger-panel">
                                    <!-- stejne menu webu jako v horni liste -->
                                    <t t-foreach="website.menu_id.child_id" t-as="ev_m">
                                        <a t-att-href="ev_m.url" class="ev-burger-link" t-att-target="'_blank' if '://' in ev_m.url else False" t-att-rel="'noopener' if '://' in ev_m.url else False">
                                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                                <polyline points="9 18 15 12 9 6"/>
                                            </svg>
                                            <span t-field="ev_m.name"/>
                                        </a>
                                    </t>
                                </div>
                            </details>
                        </nav>
                    </div>
                </div>

                <!-- =========================== HERO =========================== -->
                <section class="ev-rezhero o_colored_level">
                    <div class="ev-container">
                        <span class="ev-rezhero-badge">
                            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                                <rect x="3" y="4" width="18" height="18" rx="2"/>
                                <line x1="16" y1="2" x2="16" y2="6"/>
                                <line x1="8" y1="2" x2="8" y2="6"/>
                                <line x1="3" y1="10" x2="21" y2="10"/>
                            </svg>
                            RESERVATION SYSTEM
                        </span>
                        <h1>Book your appointment</h1>
                        <p class="ev-rezhero-lead">Select a free appointment with our doctors online, whenever it suits you. If you are unsure, call the reception and we will book you in ourselves.</p>
                        <div class="ev-rezhero-meta">
                            <a t-attf-href="tel:#{ev_web.phone or '+420722599699'}" class="ev-rezhero-chip">
                                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/>
                                </svg>
                                <t t-out="ev_web.phone_display or ev_web.phone or '+420 722 599 699'"/>
                            </a>
                            <span class="ev-rezhero-chip">
                                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <circle cx="12" cy="12" r="10"/>
                                    <polyline points="12 6 12 12 16 14"/>
                                </svg>
                                <t t-foreach="ev_hodiny" t-as="ev_h"><t t-if="ev_h_index"> · </t><span t-out="ev_h.dny()"></span> <span t-field="ev_h.time_text"></span></t>
                            </span>
                            <a t-att-href="ev_web.map_url or 'https://www.google.com/maps/place/ELITE+Veterin%C3%A1rn%C3%AD+Klinika/@50.2324691,12.8591764,17z/data=!4m6!3m5!1s0x47a0994d82cd5559:0xe9031539fb1b184c!8m2!3d50.2324692!4d12.8640473!16s%2Fg%2F11zf3w1c3h?entry=ttu&amp;g_ep=EgoyMDI2MDkxNi4wIKXMDSoASAFQAw%3D%3D'" target="_blank" rel="noopener noreferrer" class="ev-rezhero-chip">
                                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
                                    <circle cx="12" cy="10" r="3"/>
                                </svg>
                                Hybešova 167/18, Karlovy Vary
                            </a>
                        </div>
                    </div>
                </section>


                <!-- =========================== REZERVACNI SYSTEM =========================== -->
                <div class="evrez">
                  <div class="evrez-frame">
                    <div class="evrez-inner">
                      <!-- Texty jsou napevno pro kazdy jazyk, ne pres prekladovy rezim.
                           Duvod: tohle hlaseni lezi POD iframem, takze je v Odoo Translate
                           neviditelne a nikdo by se na nej neproklikal. Novy jazyk = novy radek. -->
                      <div class="evrez-nacitani" aria-hidden="true">
                        <span class="evrez-spinner"></span>
                        <span t-out="ev_web.booking_loading_title or 'Načítáme rezervační systém…'"></span>
                        <span class="evrez-nacitani-podtext" t-out="ev_web.booking_loading_note or ''"></span>
                      </div>
                      <!-- Zamerne BEZ loading="lazy": rezervace je hlavni obsah stranky,
                           nema smysl s ni cekat, az k ni nekdo doscrolluje. -->
                      <iframe t-att-src="ev_web.booking_url or 'https://reservation.winvet.cz/booking/f5832eae07850edb2b5a1167843ef423'" title="Elite Vet booking system" scrolling="no" loading="eager"></iframe>
                    </div>
                  </div>
                </div>
                <!-- Text pod rezervaci. Zamerne NENI v hlasce o nacitani: ta lezi pod
                     iframem a zmizi, jakmile se WinVet vykresli. Tenhle ma zustat. -->
                <!-- Vychozi misto textu je tady, pod rameckem. Skript nize ho na dobu
                     nacitani presune dovnitr a po nacteni WinVetu vrati zpatky. -->
                <div class="evrez-zprava-misto">
                    <p t-if="ev_web.booking_loading_middle" class="evrez-zprava" t-out="ev_web.booking_loading_middle"></p>
                </div>
                <!-- Kdo si neni jisty, ke kteremu lekari se objednat, se odsud dostane
                     na rozpis sluzeb. url_for doplni jazykovou predponu, bez ni by klik
                     v in-app prohlizeci Instagramu skoncil zpatky v cestine. -->
                <section class="evrez-rozpis o_colored_level">
                    <p class="evrez-rozpis-lead" t-out="ev_web.schedule_link_lead or ''"></p>
                    <a t-att-href="url_for('/rozpis-lekaru')" class="evrez-rozpis-btn">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <rect x="3" y="4" width="18" height="18" rx="2"/>
                            <line x1="16" y1="2" x2="16" y2="6"/>
                            <line x1="8" y1="2" x2="8" y2="6"/>
                            <line x1="3" y1="10" x2="21" y2="10"/>
                            <line x1="8" y1="14" x2="16" y2="14"/>
                            <line x1="8" y1="18" x2="13" y2="18"/>
                        </svg>
                        <span t-out="ev_web.schedule_link_button or 'Rozpis služeb'"></span>
                    </a>
                </section>

                <!-- =========================== FAQ =========================== -->
                <section id="faq" class="ev-faq o_colored_level">
                    <div class="ev-container">
                        <div class="ev-faq-head">
                            <div class="ev-badge">Frequently Asked Questions</div>
                            <h2>What do you ask most often</h2>
                        </div>
                        <!-- Caste dotazy z adminu (Web kliniky -> Caste dotazy). -->
                        <div class="ev-faq-list">
                            <t t-foreach="ev_dotazy" t-as="ev_d">
                                <details class="ev-faq-item">
                                    <summary>
                                        <span class="ev-faq-q" t-field="ev_d.name"></span>
                                        <svg class="ev-faq-arrow" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                                    </summary>
                                    <div class="ev-faq-a" t-field="ev_d.answer"></div>
                                </details>
                            </t>
                        </div>
                    </div>
                </section>

                <!-- =========================== AUTORSKY KREDIT =========================== -->
                <div class="ev-footer-credit">Created <a href="https://www.michalvarys.eu/chytil-jste-se-do-site" target="_blank" rel="noopener">Michal Varys</a> · runs on <a href="https://www.varyshop.cz/jak-vypada-nas-system" target="_blank" rel="noopener">Varyshop</a></div>

            </div>
        <script>(function(){
        var misto=document.querySelector(".evrez-zprava-misto");
        var zprava=document.querySelector(".evrez-zprava");
        var nacitani=document.querySelector(".evrez-nacitani");
        var ram=document.querySelector(".evrez-inner iframe");
        if(!misto||!zprava||!nacitani||!ram){return;}
        function dolu(){if(zprava.parentNode!==misto){misto.appendChild(zprava);}
        zprava.classList.remove("evrez-zprava--nacitani");}
        nacitani.appendChild(zprava);
        zprava.classList.add("evrez-zprava--nacitani");
        ram.addEventListener("load",dolu);
        window.setTimeout(dolu,20000);
        })();</script>
        </t>
    </template>

    <record id="rezervace_page" model="website.page">
        <!-- Nazev se zamerne nenastavuje: website.page pri prejmenovani prepise
             i klic pohledu, takze by sablona modulu po kazdem upgradu prisla
             o svuj klic (elite_vet_web.rezervace -> website.rezervace). -->
        <field name="url">/rezervacni-system</field>
        <field name="view_id" ref="rezervace"/>
        <field name="is_published" eval="True"/>
        <field name="website_indexed" eval="True"/>
    </record>

</odoo>
